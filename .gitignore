# mdBook output
book/

# bootstrap intermediates (bootstrap.sh emits these; the .seed.ll /
# .seed.runtime.c + src are tracked). compiler/plewc itself is tracked.
/compiler/plewc0
/compiler/plewc.ll
/compiler/plewc.runtime.c
/compiler/plewc.check.ll
/compiler/plewc.check.runtime.c

# the dependency resolver binary (built on demand by `plew`; the source
# compiler/resolve/_.pw is tracked).
/compiler/plew-resolve

# scratch dir for local experiments (gen-harness builds, self-host checks, etc.)
/tmp/
