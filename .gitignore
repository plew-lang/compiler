# mdBook output
book/

# selfhost build artifacts (the compiler emits a binary + .c next to each .pw)
selfhost/*
!selfhost/*.pw
# ...except the bootstrap seed (the checked-in C the build starts from)
!selfhost/plewc.seed.c
