# 作業ログ

> 現在地・次の一歩・運用メモ・再利用資産だけを置く。完了した「やった」は git（記述的タグ）へ。「なぜ・却下案」は [design-decisions.md](design-decisions.md)、パイプライン設計は [architecture.md](architecture.md)、spec からの意図的剥離・暫定は [provisional.md](provisional.md)、メタプロの段取りは [metaprogramming-architecture.md](metaprogramming-architecture.md)。

## 現在地

🎉 **セルフホスト達成・stage0（Rust）退役済み。** 正典コンパイラ＝Plew パッケージ `compiler/src/`（root `_.pw` が `part` で全パートを綴じる 1 モジュール）。自分自身を不動点までコンパイル。hidden-meaning（整数幅・match 網羅・ラベル・受理の健全性・値意味論/CoW・`unique`/`deinit`/move・generics・トレイト＋Eq/Ord＋derive＋関連型・クロージャ）は概ね解消。**残る剥離・暫定は [provisional.md](provisional.md) が正典**。

**構文解析は完全に `@Std/Syntax` の責務（A＝フロントエンド統合・完了）。** コンパイラとマクロは**唯一の共有パーサ**を通る＝ドリフトする文法に二重パーサなし。

- **`@Std/Syntax`（構文＝form）**：レクサ `Syntax/Lexer.pw`／値ツリー AST `Syntax/Ast.pw`＋`Syntax/Trees.pw`（String 名・全ノード原本座標 span）／パーサ `Syntax/Parser.pw`（宣言・トップレベル）＋`Syntax/ParseBody.pw`（式・文・パターン・ブロック）／出力ビルダ `Syntax/Build.pw`（`Src`）。入口は `parseProgramAst(p) -> Array[TopItemAst]`（プログラム全体）と `parseItem(source,start,end) -> TopItemAst`（マクロ用＝注釈対象 1 項）。
- **コンパイラ（意味＝meaning）**：`compiler/src/`＝`Loader`（パース駆動ローディング＋パス解決）・`Ast`（arena ノード＋`Comp`）・`Parser/Decl`（＝module-tag helper `markImport`/`recordExport`＋derive 合成 `synth*`。**宣言パーサは持たない**）・`Codegen/`〔Emit/Resolve/Ops/Check/Expr/Stmt/Decl/Mono/Array/Async/Gen/**Lower**〕。`Codegen/Lower.pw` が共有値ツリー（TopItemAst/DeclAst/ImplAst/…）を arena へ lower。
- **ローディングもパース駆動**：driver（`main`）が各ファイルを `c.bytes` に追記→`parseAndLowerFile`（共有パーサで parse＋lower）→返った木の `Import` ノード（import＋part）を辿って子ファイル探索（ハンドロールの事前スキャンなし）。残る I/O（読込・パス解決）と arena バイト配置・module tagging はコンパイラ側＝構文解析でない部分のみ。

完了した大物（詳細は git・記述的タグ）：async/await 段階 1-3（stackless SM）・コアライブラリ境界（`@Std` の `extern "plew-intrinsic"`）・**Array＝`RawBuffer` 床の Plew struct**（[array-struct-plan.md](array-struct-plan.md)）・可視性完全強制・Iterator/Iterable＋lazy map/filter・再帰値型 auto-boxing＋ARC・renovate Phase A（ARC 1 本化・Op enum・expect）＋Phase B B0-B2（型付き AST キャッシュ）＋**Phase C＝名前 interning（スコープ版・宣言名スキャンを整数比較へ）**・**メタプロ M0/M1**（マクロが実宣言を読んで生成・tag `metaprogramming-m1`）・**A＝フロントエンド統合**（全パース→共有 @Std/Syntax・パース駆動ローディング）・**マクロ入力 `TopItemAst`**（struct/enum/fn に加え **impl/trait** もマクロ対象）・**パターン/構築のフィールド実在検査**（`checkMatchBinds`/`checkMakeFieldExists`＝受理の健全性）・**Eq/Ord トレイト＋`@[Eq]`/`@[Ord]` derive**・**Hash/Hasher＋SipHash-1-3＋`@[Hash]` dogfood**・**Dictionary[K,V] lang item（struct+メソッド+添字+リテラル `[k:v]`/`[:]`）**・ネスト generic 単相化（`Array[Gen[K,V]]`）・generic コンストラクタ型引数推論（JSX）・**missing-return 検査**（非 `()` 関数の全経路 return 強制）・**`print(String)`**（String:Format＋リテラル引数の T 推論）・**演算子トレイト配線 O1-O8**（算術/ビット/単項/比較をユーザー型＋プリミティブで witness 脱糖・**built-in 算術完全削除**・`??` 演算子廃止→`unwrapOr`・Index/IndexSet・Chain `?.`・真の負リテラル畳み込み・同名トレイト構造的解決）・C 予約語マングル等の細バグ群。renovate vs rewrite の判断・負債の地図は [architecture.md](architecture.md)「負債監査の結論」、設計根拠は [design-decisions.md](design-decisions.md)、再利用機構は本書末尾「再利用資産・罠」。

## 次の一歩

**直近の到達点＝演算子トレイト配線が一段落（spec/12・O1-O8）**。算術 `+ - * / %`・ビット `& | ^ << >>`・単項 `- ! ~`・比較 `== != < <= > >=` をユーザー型＋**プリミティブ**ともに witness 脱糖し、**built-in 算術は完全削除**（全演算が `@Std/Core` の witness→intrinsic 床経由・typeOf を context-typed form まで完成・no-Core/width-less は loud error）。`??` は演算子廃止し `Optional.unwrapOr(fallback:)`（eager 値/lazy クロージャのオーバーロード）へ。Index/IndexSet（ユーザー型添字 `a[k]`/`a[k]=v`）・Chain `?.`（Optional 具体・短絡・連鎖）も実装。設計根拠・却下案は [design-decisions.md](design-decisions.md)、残ギャップは [provisional.md](provisional.md)、機構の罠は本書末尾「再利用資産・罠」。

**演算子の残り（小・additive）**：一般 Chain トレイトディスパッチ（現 Optional 具体）・`Pow`〔float 実装後〕・複合添字 `a[k] += v`（ユーザー型 get-modify-set）・`Output != Self`。

**➡ アクティブ＝バックエンド LLVM 化（libLLVM-C を叩く）。FFI 設計確定（spec/15「外部コード統合」・design-decisions「外部コード統合」）＋最小サブセット実装・検証済。**
- **FFI 設計（使う側のみ・spec/15）**：`extern(c)` import／`repr(c) struct` 共有レイアウト／`CPtr[T]`(read)/`CMutPtr[T]`(write)/`COpaque`(void\*)／不透明ハンドル `extern(c) { type Name }`／NULL は生ポインタの性質＝取り出しが `Optional`（`.toOptional()`/`load()`・境界写し機構は廃止）／所有権は `unique`+`deinit`／ABI はクォートなし bareword（`extern(c)`・閉じた集合）／リンクは自己完結 C 出力＋手フラグ interim（manifest は M3）。使わせる側（export＝Plew→C）は未定（既存 `export` と要整合）。
- **最小サブセット実装済（コンパイラ）**：`extern(c)`/`extern(plewIntrinsic)` の bareword パレン ABI パース（string `"plew-intrinsic"` も互換維持）＋`type Name` 不透明ハンドル（`typedef void* Name;` 自己 emit・`#include` 不要）＋C-extern は生名で呼び・プロトタイプ自己 emit（`isCExtern`＝`isExtern` の細別）＋`CPtr[T]`/`CMutPtr[T]`→`T*`・`COpaque`→`void*`＋文字列床 intrinsic `cString`/`stringFromCString`（プリアンブル `plew_cString`/`plew_stringFromCString`）。**検証**＝tests/run/ffi_extern_c（libLLVM-C 形スタブ `.c` をリンク＝test.sh が随伴 `.c` を自動リンク・系の回帰ガード）。実 libLLVM-C での検証は下記「first light」で達成済（本機に Homebrew LLVM 22 インストール済）。
- **`extern(c)` ラベルなし（決定・実装済）**：C 関数に引数ラベルは無いので extern(c) fn は全引数ラベルなし（positional）。**暗黙に剥がさず `~:` 明示必須**（素のラベルはコンパイルエラー＝no hidden meaning）。lowerExtern が `noLabel` 検査。reject test ffi_extern_c_label。ラベルは Plew ラッパの皮に被せる（床/皮分離）。
- **実 libLLVM-C で IR 構築＝達成（FFI first light）**：Homebrew LLVM 22（libLLVM-C）を Plew が `extern(c)` 経由で駆動し**本物の LLVM IR を構築・出力**（`define i32 @add(i32 %0, i32 %1) { ... %sum = add i32 %0, %1  ret i32 %sum }`）。**`@Std/Ffi`**（`compiler/std/Ffi.pw`＝`cString`/`stringFromCString`/`arrayCPtr` の床 intrinsic・export）＋**`arrayCPtr[T]: Array[T]→CPtr[T]`**（`(a).data`＝RawBuffer 基底を `CPtr` 化＝ハンドル配列 `LLVMTypeRef *Params` 渡し）を実装。**バインディング＝`examples/llvm/Llvm.pw`**（IR 構築サブセットの `export extern(c)`・1:1 with `<llvm-c/Core.h>`）＋デモ `examples/llvm/BuildAdd.pw`＋README（ビルド/リンク手順）。リンク＝`clang out.c $(llvm-config --ldflags --libs core) -Wl,-rpath,$(llvm-config --libdir)`（`#include` 不要＝自己完結 C）。クロスモジュール export/import（不透明型＋関数）も確認。
- **方式確定（2026-06-08・ユーザー承認）＝最初から in-process libLLVM-C ＋ 別バイナリ**。textual `.ll` 先行は総工数を下げない（SSA 構築ロジック＝作業の9割が共通・textual は表面層を作り捨て版脆弱）ため不採用。**バイナリ構成**：`compiler/plewc`（Cバックエンド・`_.pw`）は libLLVM を一切参照せず **clang だけのブートストラップを維持**。新ルート `compiler/src/_llvm.pw`（同じフロントエンド part 群＋LLVMバックエンド part `Backend/Llvm`）を既存 plewc がオンデマンドでビルド→`compiler/plewc-llvm`（libLLVM をリンクするのはこのバイナリのみ・種/不動点/bootstrap.sh 無変更）。立ち上げ出力は当面 `LLVMPrintModuleToString`→clang（構築は本物API・オブジェクト直接出力への差し替えはAPI1呼び出し）。
- **共有フロントエンド切り出し**＝`Codegen/Driver.pw`（`runFrontend() -> Comp`＝parse→check→mono→typed-AST／`emitC(inout Comp)`＝Cバックエンド emit）。`_.pw` main = `runFrontend()+emitC()`、`_llvm.pw` main = `runFrontend()+emitLlvm()`。`_.pw` は LLVM emitter を一切参照しない＝plewc は libLLVM フリー。
- **ビルド/テスト**：`./build-llvm.sh`（plewc が `_llvm.pw` を C 経由でビルド→clang＋llvm-config フラグ）。`./test-llvm.sh`＝LLVM バックエンドを tests/run に走らせ stdout を**C と同じ golden** に照合・未対応機能は loud reject で **SKIP** 計上（=ライブ網羅率＋回帰ガード）。companion ランタイムは `plewc-llvm --runtime`（scalar/pointer シグネチャのみ＝struct-by-value ABI 回避）。
- **現網羅（`./test-llvm.sh` pass=25 skip=143 fail=0）**：エントリモジュール（id 0）の `main`＋free 関数（引数/戻り/再帰/デフォルト引数/レキシカルスコープ・shadowing）・**幅正確な整数**（i8/i16/i32/i64＝リテラル幅は suffix か checker の `exprIntCtx`〔typeOf 経由〕で決定・typed sink〔annotated let/call arg/default arg/return/field/struct-field〕で trunc/sext/zext coerce・ランタイム境界〔print/exit〕は i64 へ拡張・overflow-panic は TODO）・**全演算子**（算術/ビット/シフト/単項/比較・`>>` は算術＝unsigned 論理シフトは TODO）・Bool・String（値 `{ptr,i64}`・リテラル・`==`/`!=`）・`if`/`while`・intrinsic exit/write/eprint/print/panic（**print は引数の型 kind でランタイム formatter に分岐**＝Format トレイト機構を迂回・観測同一）・**struct**（全フィールド primitive＝scalar/String/Bool の非 generic 非 extern 非 unique を named LLVM struct 登録・JSX 構築 insertvalue・フィールド read extractvalue/write GEP+store・struct ローカル/引数/戻り＝LLVM 間 by-value）・**メソッド**（self ディスパッチ・by-value self は alloca に格納/inout self は pointer param・field access は locPtr を一様に GEP・メソッド呼びは findMethod 解決＋self 先頭引数・**エントリモジュール限定**＝body 先頭文の原本 offset で判定）。**ローカル型 kind タグ＋owning struct index**（`c.locals` 非投入でも String/符号/struct 判定が効く）。**未対応は loud reject**＝struct の Eq/Ord/Format（比較/print）・nested/array-field/unique struct・enum/array/generics/closure/async は拒否（silent miscompile なし）。
- **設計上の要点（罠）**：①LLVM モジュールは自己完結ゆえユーザー関数のシンボル名は内部一意でよい＝`pf<idx>` 合成名・**呼び出しは解決済 func index→キャッシュ `LLVMValueRef`** でディスパッチ（名前マングル不要）。②Plew に narrowing `as` が無い（U64→U32 不可）ため、LLVM API の count/index 系 U32 パラメータは**バインディングで U64 宣言**（LP64 で下位32bit が読まれ小さい値は安全）＝narrowing（wrapping/TryFrom）実装後に見直し。③`extern(c)` 関数は `~:` positional・`fn` は予約語（変数名に使えない）。④全ユーザー関数を出すと std 全体（Prelude/Core の shift 等未対応機能）を引き込むので**当面エントリモジュール限定**＝将来は call-graph 到達可能性で必要な std だけ引く。
- **済（pass=47）**：整数幅(v5)・struct(v6)・メソッド(v7)・**enum+match**(v8＝タグ付き union `{i64 tag,[maxWords x i64] payload}`・match は tag compare/branch chain＋payload extractvalue・value-position match も PHI)・**value-position if/match＋`as`キャスト＋幅正規化**(v9)・**Array**(v10＝`{ptr,i64}`＋memcpy ベースの element-size generic runtime〔`plew_rawbuf_alloc`/`plew_arr_grow`/`plew_bounds`・typed GEP で per-elem load/store・`LLVMSizeOf`〕・リテラル/index r-w/count/append/反復・配列ローカル/引数/戻り・**per-local elemTy+elemKind**でキャスト/print の幅・符号正しく・配列リテラル引数/デフォルトは param の elemTy で構築)。**未対応は loud reject**（nested `Array[Array[T]]`・struct の Eq/Ord/Format・generic・closure/async）。**CoW（配列コピーの値意味論）未実装**＝cow_array/let_infer は test-llvm.sh の `KNOWN_GAP` で別計上（fail でなく cow-gap）。
- **済（pass=49・cow-gap=6）追加**：v11＝**array-field struct**（struct フィールドに Array・`s.items[i]`/`.count`/`.append` は struct フィールドを GEP した addressable place 経由）＋**inout パラメータ**（`c: inout Counter`＝ポインタ受け・inout self と同じく locPtr 直結・呼び側は place アドレス渡し）、v12＝**符号別演算**（buildBinOpS＝unsigned で udiv/urem/unsigned-pred/lshr）、v13＝**struct ==/!=**（フィールドごとの構造的 Eq＝int は icmp・String は byte 比較・AND・array/enum フィールドは reject）＋user Index/IndexSet subscript（`g[i]`）は crash せず loud reject。**値意味論 CoW 未実装**＝配列/struct コピーで buffer 共有＝cow_array/let_infer/cow_struct/value_semantics_* の6本は `KNOWN_GAP`（cow-gap 別計上）。
- **済 追加**：v14＝**for ループ**（range `lo..<hi`/`..=`・array `for x in arr`・struct-destructure と Iterable は reject）、v15＝**generic structs**（`Box[I32]` 等＝monomorphization 基盤確立）。**generics 基盤（v15・重要・以降の型紋様）**：①`llvmScalarTy` に**型パラメータ置換**＝env `c.curTypeParams`→`c.curTypeArgs`（nameId 一致で concrete TypeRef の head へ・非 generic では no-op）。②`registerGenStructs`＝`c.genInsts`〔front-end が収集した具体インスタンス＝TypeRef index〕を走査・`isGenericInst`／`genericStructIndex` で generic struct を判定・env を `(def.typeParams, instance.args)` に set→named LLVM 型を**substituted フィールドで build**→`genStructRefs/genStructTys/genStructDefs` に登録（`typeRefEq` で照合）。③generic struct ローカルは **`locStructIdx`=generic def・`locTypeRef`=instance ref**〔`pushLocalR`〕＝フィールドアクセスは既存 struct machinery を再利用。④構築は env を set して `buildStructValue`（plain と共有）。
- **済 追加（generics 三本柱完了）**：v16＝**generic enums**（Optional/Result＝`registerGenEnums`・match/MatchExpr は scrut の `locTypeRef` から `setEnumEnv` で env 復元・MatchExpr/IfExpr の arm は first arm 幅に unify）、v17＝**generic free functions**（`c.fnInsts` ごとに `declareGenFns`/`emitGenFns`＝env 下で signature/body・呼びは `inferFnArgs`→`genFnSlotFor`〔typeArgsEq〕→env 下で arg build→call・**エントリモジュール限定**〔std generic fn は body 未対応・print は call site 特殊化で instance 不要〕・`genLlvmFunc`→`genLlvmFuncBody(fi,fv)` に分離）。`paramLlvmTy`/`llvmFnRetTy`/`localStructIdxFor` も env 置換対応（generic param の method dispatch 等）。**generic param/return/field の instance 解決＝`llvmTypeOfRef`**。
- **generics の残り（既知ギャップ・additive）**：generic_free_fn 等の **`where T: Trait` 境界 + 型パラメータ値へのメソッド呼び**（`step[T](x:T) where T:Getter { x.get() }` の call が未解決＝genFnSlotFor で instance 不一致 or trait-bounded dispatch 要）。**generic methods**（recvInstRef != 0 の fnInst・現状 free fn のみ）。generic struct/enum を**さらに関数/フィールドに深く流す**ケース。
- **済 追加（v19–v23・pass=60→73）**：v19＝**nested struct**（struct-of-struct＝registerStructs 2-pass〔名前先行→body 後置〕・`structRegisterable` 再帰・`exprStructIdx` に Field ケース）。v20＝**generic methods**（generic struct/enum レシーバのメソッド＝`st.genMeth*` レジストリ〔(method fn, recv inst ref)〕・`declareGenMethods`/`emitGenMethods` が genInsts×マッチ〔非 provided・entry〕メソッドを recv env〔`st.curRecvInstRef`〕下で emit・`genLlvmFuncBody` が self の型/struct-idx/env を curRecvInstRef から復元・`genLlvmMethod` が local の inst ref で dispatch・generic 構築/戻り/param は `groundTypeRef` で env grounding）。v21＝**generic free fn に struct 引数**（front-end `inferFnArgs` は typeOf で locals 不可視→`llvmInferFnArgs`/`llvmArgTypeRef` が LLVM 側 local 追跡で arg TypeRef 復元・`exprStructIdx` に Call ケース〔call 結果メソッド `firstOf(...).get()`〕）。v22＝**enum ==**（payloadless はタグ比較・enum は AST から判定〔opaque LLVMTypeRef は `==` 不可〕・payload enum は switch で deferred）＋**struct Ord**（`< <= > >=`＝`emitStructCmp` が後→前 select chain で i32 ordering・scalar/nested struct・String/array/enum field deferred・`LLVMBuildSelect` binding 追加）。v23＝**assoc fn**（`Type.name()`＝no self dispatch・**到達可能性ゲート**〔`collectReachableAssoc`＝非 assoc body から seed→reached assoc body を fixpoint・derive 生成の未呼び witness を emit しない〕・`isPlainUserFn(fi)` 化・`exprStructIdx` に Method ケース）。
- **済 追加（v24–v26・pass=73→77）**：v24＝**user Index/IndexSet**（`g[i]`→`g.index(key:i)`／`g[i]=v`→`g.indexSet(key:i, value:v)`＝`synthDictMethod` で labeled Method 合成→通常メソッド dispatch・複合 `g[i] OP= v` は `v`→`g[i] OP v` 先行 desugar・indexSet の inout self は placePtrOf で in-place）。v25＝**String.bytes ビュー**（String=`{i8*,i64}`＝Array[U8] と同レイアウト→`s.bytes` は String 値そのもの〔no copy〕・`isStringBytesField` を exprIsArray/arrElemTyOf/exprArrElemTy/arrElemKindOf/field-read に配線・無注釈 array 束縛 `val bs=s.bytes` も isArr 追跡）。v26＝**プリミティブ演算子 witness**（`I32.add(lhs:,rhs:)`/`I64.neg(value:)`/`U8.bitnot(value:)` 等を `genPrimOpWitness` で直接 LLVM op に〔std witness body を emit せず〕・add..shr は buildBinOpS・neg/bitnot は Neg/Not・型/符号は受け手プリミティブ）。
- **済 追加（v27–v28・pass=77→80）**：**Ref[T]**（`<Ref[T] value=e/>`＝`rawbufAlloc(sizeof T,1)` に pointee store→cell ポインタ返す・`Ref[T]` ローカル/パラメータ=opaque ptr＋locTypeRef に Ref instance 追跡〔`isRefInst`＝Mono.pw 既存・Ref は登録 generic でないので `isGenericInst` は false・専用判定要〕・copy=ポインタ copy で alias 共有・`r->field` 読み=pointee struct load→extract〔`loadRefPointee`/`refPointeeStructIdx`/`refPointeeTy`〕・`r->field = v` 書き=cell に StructGEP→store〔alias 越し可視〕・array field の buffer 共有で `r->arr[i]=v` も可視・Arrow を genLlvmExpr/exprIsArray/arrElemTyOf/Assign に配線。**refcount retain/release＋pointee deinit は hidden cost で deferred**＝対象に deinit なしゆえ leaky alloc でも観測的に正しい〔deinit 付き型・cycle は ARC 本実装で〕。**残**＝array-of-Ref 要素越し `ys[0]->v=v`〔array element-typeref 追跡が要〕）。
- **済 追加（v29・pass=80→81）**：**payload enum ==**（enum 値を `{tag,payload}` 丸ごと zero-init〔`LLVMConstNull(ety)` 先行 store〕→payloadless variant の payload も決定的に・payload が全 scalar int/Bool の enum〔`enumEqWordable` ゲート〕は tag＋payload word の bit 等価〔`emitEnumEqWords`／`enumMaxWords`〕・String/struct/array payload は deferred・**Ord は未**〔符号付き負値・word 内 field 境界で hack 不可ゆえ per-variant switch が要〕・**enum ローカル追跡**＝非 generic enum local に bare enum TypeRef を locTypeRef へ〔typeOf は local 不可視〕・`exprEnumIdx` が locTypeRef 経由で復元）。
- **済 追加（v30・pass=81→82）**：**un-annotated generic binding の instance 推論**（`val r = makeBox()`〔makeBox -> Box[I64]〕で注釈なし local の generic instance ref＋def struct idx を call return から復元＝`exprResultRef`〔generic free-fn return は推論型引数下で `groundTypeRef`〕を Let〔注釈なし枝〕に配線→`r.v` が T を置換。unblocks generic_ctor_infer。**残**＝nested generic field grounding〔`bb.v.v`：v: Box[I32] 自体が generic instance＝generic_struct_multi〕）。
- **済 追加（v31・pass=82→85）**：**struct/enum 演算子トレイト witness**（二項算術/ビット/シフト・前置単項を user struct/enum オペランドで witness assoc fn へ＝`a + b`→`Vec2.add(lhs:,rhs:)`・`-money`→`Money.neg(value:)`・C backend の emitBinTraitCall 対応。右オペランドオーバーロード〔Add[Vec2] vs Add[I64]〕は **rhs 型を LLVM env から復元**して解決〔`binWitnessFn`＝findBinTraitWitness の LLVM 版・typeOf は local 不可視ゆえ自前 disambiguation・rhs が struct/enum なら exact、primitive なら int overload〕・`exprStructIdx` が witness 返り型を解決〔`(-m).cents`/`(a+b).x` 連鎖〕・witness は**名前で reachable seed**〔add/sub/.../neg/not/bitnot＝eq/compare と衝突せず derive Eq/Ord 除外維持〕＝演算子脱糖は scan する call node を残さないため。unblocks operator_unary/operator_bitwise/operator_trait_decl。**残**＝overflow→panic は primitive と同じく未〔raw LLVM op〕）。
- **済 追加（v32・pass=85→86）**：**wrapping 整数メソッド**（`x.wrappingAdd(by:)`/`wrappingSub`/`wrappingMul` を primitive 整数レシーバで受信幅の plain LLVM add/sub/mul に＝二の補数 wrap は LLVM 既定・checked overflow 未モデルゆえ現状 `+` と同義だが wraparound 正しく受理。struct メソッド fallthrough の前に配線。unblocks wrapping_arith）。
- **済 追加（v33・pass=86→87）**：**struct 分解パターン match**（`match s { S { val x, f: val y, g: _ } => … }`＝irrefutable〔単一形・tag なし〕＝scrutinee を slot に store→各 named field を bind〔array/nested-struct/型パラメータ field 型を honor・generic 時は env を `setStructEnv` で設定〕→arm 出力。statement match〔genLlvmMatch〕・value-position match〔genLlvmMatchExpr〕両対応。`bindStructPattern`＋`setStructEnv` 追加。unblocks struct_pattern）。
- **済 追加（v34・pass=87→88）**：**struct 分解 for ループ**（`for S { val x } in arr`＝struct 配列を走査し各要素の field を毎反復 bind〔反復末で pop〕・match struct パターンと対称。`bindStructPattern` を binds 直接受け取りに refactor。unblocks for_struct_destructure）。
- **済 追加（v35・pass=88→89）**：**Array prelude get/set**（`arr.get(i:)`＝checked 読み〔`arr[i]` 同等〕・`arr.set(i:,value:)`＝checked 書き〔shared buffer 越し place mutation〕・`genLlvmArraySet` 追加。unblocks prelude_array。**残**＝Array[T] への user 定義メソッド〔first/second/size＝generic-method-on-Array 単相化が要・generic method dispatch は Array 除外中〕）。
- **済 追加（v36・pass=89→90）**：**payload enum Ord**（`< <= > >=` を tag-then-payload lexicographic compare へ＝tag を先に〔小さい tag が小〕・tag 同値時に active variant の payload を比較〔payload 域を variant の typed payload struct として再解釈し各 field を `cmpFieldOrd`＝符号正しい〕・全 payloaded variant の compare を計算し tag で select・`enumOrdSupported` でゲート〔scalar/Bool/nested-orderable-struct〕。**enum param 追跡**＝非 generic enum param に bare enum TypeRef〔以前は enum local のみ〕で `a<b`/`a==b` on params 解決。`emitEnumCmp`/`emitVariantPayloadCmp`/`emitEnumOrdOp`/`enumOrdSupported` 追加。unblocks derive_ord_enum。**enum 演算子は == と Ord 両方〔payloadless/payload〕完備**）。
- **済 追加（v37・pass=90→92）**：**再帰 value enum boxing**（再帰 enum field〔enclosing enum 型の payload field・front-end `c.boxedFields` が印付け〕を heap pointer〔1 word〕に＝型を有限化。`enumAllPrimitive` が boxed field 許容・`fieldWordsF`/`variantPayloadTy` が pointer slot・`buildEnumValue` が cell heap-alloc〔rawbufAlloc〕＋値 store＋pointer を payload に・`bindMatchPayload` が pointer を deref して boxed 値を load＋enum-typed bind に bare enum TypeRef。refcount/free 未〔leaky・Ref 同様・観測的に正しい〕。unblocks rec_list/rec_tree）。
- **済 追加（v38・pass=92→94）**：**再帰 struct-field boxing＋generic-enum 構築推論**（①再帰 struct field〔Node.next: Optional[Node]・相互再帰 Term/Sum〕も boxing＝`structRegisterable` が boxed field 許容〔boxed skip で cycle 切断＝相互再帰も停止〕・body は pointer slot・`genFieldValue` が cell heap-alloc＋store・field read は pointer deref〔pointee 型は field TypeRef から＝generic instance field 解決〕・`fieldWords` の struct-field 合算も boxed-aware。②**generic-enum JSX 構築の型引数推論**〔`<Optional.Some v=42 />` 明示 `[I64]` なし〕＝expected-type context `st.expectedTy`〔let 注釈・boxed-field pointee で set・`genLlvmMakeEnum` が consume＝clear で payload 副式に漏らさない〕。③generic-instance 型の struct field〔node.next〕は `exprTypeRef` が instance ref を返す＝field read への match が enum の substitution env を復元〔payload を具体型で bind〕。unblocks rec_struct_optional/rec_mutual。**残**＝iter_* は closure＋Iterator protocol が要〔boxing でない〕・unique_* は deinit/move〔ARC〕が要）。
- **済 追加（v39・pass=94→96）**：**非キャプチャ closure＋array 値 return 修正**（closure＝`{fn, env}` fat pointer〔`st.closureTy`〕・`fn(...)->...` 値はこの型・各 Closure 式を `__closure<id>(ptr env, params...)` に lambda-lift〔env 先頭・非キャプチャは NULL〕＝`collectClosures` で到達 closure を宣言〔nested も fixpoint〕・`emitClosures` で body 出力・closure literal は `{fn=__closureN, env=null}` を build・closure 型 local への call は indirect call〔fn TypeRef からシグネチャ復元〕・`bindOneParam` 抽出〔fn と closure で共有〕。**array 値 return/param 修正**＝`Array[T]` は front-end では generic-struct instance だが LLVM の array 機構は arrayTy `{ptr,i64}` を使う＝`llvmTypeOfRef` が `Array[T]`→arrayTy にマップ〔別 %gsN を吐いて miscompile していたのを修正〕。unblocks closure_literal/higher_order）。
- **済 追加（v40・pass=96→97）**：**キャプチャ closure〔by-value〕**（env struct を heap-alloc〔captures を宣言順〕・literal で各 captured 値を copy・body で各 capture を env slot から fresh local に load〔識別子アクセスが普通に解決・generic/enum 型情報も追跡〕。`mut val`〔boxed・共有 cell〕capture は未モデル＝`closureSupported` ゲートで未宣言にし literal が loud reject〔clean unsupported〕。unblocks closure_capture）。
- **済 追加（v41・pass=97→98）**：**closure の struct/String/array heap 値 capture**（captureSlotTy が各々を正しい LLVM 型に・env build が load/store・body が typed local に bind）。tests/run/closure_capture_heap.pw の `;` struct を**改行区切りに修正**（`;` は Plew トークンでない＝spec/05 struct フィールドは改行区切り・共有パーサが `;` を**幻フィールド**にして C backend が silent miscompile・LLVM は厳格 reject）。パーサ幻フィールドバグは review-items に記録〔parseStructDecl が `;` を comma 同様 loud reject すべき・reseed 要〕。unblocks closure_capture_heap）。
- **済 追加（v42・pass=98→99）**：**function-as-value thunk**（bare 関数参照〔`val g = twice`・`apply(f: twice)`〕を closure `{thunk, null}` に＝`collectThunks` が全 expr の Ident で plain free fn を名指すものに thunk `__fnthunk<fi>(ptr env, params...)` 宣言・`emitThunks` が forward body 出力〔env skip して pf<fi> 呼び〕・Ident path が `{thunk, null}` build。C backend の fn-thunk パス対応。unblocks fn_value）。
- **済 追加（v43・pass=99→100🎉）**：**closure 型 struct field を `self.f(...)` で呼ぶ**（iterator-adapter パターン＝struct が mapping/predicate closure を field に持つ・method 解決失敗時に field 型が〔generic env 越しに〕fn 型に ground するなら closure 呼びに。`emitClosureCallValue` 抽出〔closure 値への呼び・local-call と field-call で共有〕。unblocks closure_field。**tag `llvm-backend-100pass`**）。
- **closure 残**：closure_mut_capture/closure_shadow_capture〔`mut val` boxed capture＝共有 cell・`closureSupported` で clean skip 中〕・iter_*〔Iterator protocol provided method〔map/filter〕＋closure・closure_field の機構が下地〕。
- **済 追加（v44・pass=100→102）**：**provided trait method〔エントリモジュール〕**（front-end が provided method を各 conformer に copy 済〔recv=conformer・isProvided=true・body 共有〕＝`isPlainUserFn` の blanket isProvided reject を撤去し、非 generic conformer の copy を通常 method path で emit〔registered struct＋`methodInEntryModule`＝共有 body がエントリモジュールにある＝std trait の provided は除外維持〕・各 copy は自分の self 型で emit〔共有 body の `self.req()` がその型の要求に解決〕。unblocks trait_provided/trait_where）。
- **済 追加（v45・pass=102・infra）**：**return-context generic-enum 構築推論**（`return <Optional.Some v=x />`〔明示 `[I64]` なし〕が fn/closure の宣言 return 型から型引数推論＝`st.curRetRef`〔genLlvmFuncBody/emitClosures で set〕を Return が `st.expectedTy` に。let 注釈・boxed field 推論と対称。**Iterator protocol〔next()->Optional[Item]〕の下地**・単独でテストは flip しない〔for-Iterable 脱糖も要〕が実 reject を除去・両 suite green）。
- **済 追加（v46・pass=102→103）**：**for-over-Iterable 脱糖＋lazy provided-method emission**（①`for x in iterable`〔非 array〕を iterator protocol へ＝`it=iterable.iterator(); loop { match it.next() { Some(v)=>{x=v; body}, None=>break } }`＝`genLlvmForIterable`〔iter を mutable slot に・next() は inout・各 next() の Optional[Item] を tag で match・Some payload を x に bind〕。②**provided method emission を eager〔v44〕から lazy/reachability へ**＝body offset が std/entry を判別不能ゆえ、call が解決した時だけ declare＋queue〔`ensureProvidedDeclared`〕→main pass 後に drain〔`emitProvidedMethods`・fixpoint〕。未 call の std adapter method〔map/filter＝body が未対応 adapter 構築〕は emit されない＝v44 の latent bug〔main が早期 error しなくなって露呈〕を解消。unblocks iter_custom・trait_provided/trait_where 維持）。
- **済 追加（v47・pass=103→104）**：**bare Iterator の for ループ**（`genLlvmForIterable` が iterator() を持つ Iterable に加え、next() を直接持つ Iterator〔値自身が iterator〕も対応＝iterator() があれば呼んで iterator 値+struct を、無ければ値自身を iterator に。unblocks iter_for_bare）。
- **済 追加（v48・pass=104→105）**：**generic Iterable/Iterator の for ループ**（`Box[I64]`/`BoxIter[I64]`＝receiver が generic instance なら `genLlvmForGenIterable`＝iterator()/next() を monomorphized generic-method slot 経由〔genMethSlotFor/genGenMethodCall〕で。iterator instance ref は iterator() の return 型を receiver env で ground して回収・Optional[Item] は next() の return を iterator instance env で ground。共有ループ末尾を `emitIterNextLoop` に factor。また genLlvmFuncBody で `st.curRetRef` を active generic env で ground＝monomorphized body の return-context enum 構築が具体 Optional を推論。unblocks iter_generic）。
- **済 追加（v49・pass=105→106）**：**lazy generic-method emission〔std/非エントリモジュール〕**（declareGenMethods はエントリモジュール非 provided しか declare せず、std generic instance の method〔Optional.unwrapOr/isSome・Result.isOk/isErr・MapIter.next〕が reject されていた。`ensureGenMethodDeclared`＝call が到達した時に declare＋queue〔method type params が receiver instance の args と 1:1＝instance が env を完全 ground する場合のみ〕。provided queue と一緒に fixpoint まで drain〔body が相互到達〕。genLlvmMethod の generic 経路と genLlvmForGenIterable の iterator()/next() に配線。+ valueKind の generic-enum/struct method 修正〔`res.isOk()`→Bool＝exprStructIdx は enum を見えないので instance ref で resolve・以前は i1 1 を sign-ext で -1 と print〕。unblocks core_lib）。
- **済 追加（v50・pass=106→108）**：**`?.` optional-chain ＋ `try` Result 伝播**（`genLlvmOptChain`＝base Optional[T] を読み Some なら inner T の `member` を読んで Some(member):Optional[U] を再構築・None なら None・結果 instance は構造マッチで発見・chain `a?.b?.c` は exprTypeRef の OptChain branch〔`optChainResultRefLL`＝typeOf でなく LLVM 側 type tracker で解決〕で。`genLlvmTry`＝Result[T,E] を読み Err なら関数自身の Result Err を early-return〔error 型一致・From 変換は未〕・Ok なら値を yield して続行。`buildEnumFromValues`〔expr id でなく算出済 SSA payload 値から enum 構築〕を追加。unblocks optional_chain/try_coalesce）。
- **済 追加（v51・pass=108→115）＝Iterator adapter 層 完了**（iter_for_map/iter_for_filter/iter_map_retype/iter_chain/iter_chain3/iter_chain_mapmap/iter_chain_multiline の 7 本）：**inline adapter 構築**＝`xs.map(f)`/`xs.filter(p)` は provided method〔method-own 型パラ B＋associated Item〕の body を mono 化せず、call site で adapter struct〔MapIter/FilterIter〕を直接建てる〔`genLlvmAdapterWrap`＝inner=recv・残フィールド=method args・登録済 instance は inner〔arg0〕を recv 型に構造マッチで発見＝`findAdapterInst`〕。exprTypeRef に Method branch を足して for ループ/chain が iterand を型付け。**env を merge〔replace でなく〕**＝`genLlvmMakeEnum`/`genLlvmMake` で payload/field の sub-expression〔MapIter.next の `<Optional.Some v=self.f(x:v)/>`〕が enclosing method の型パラ〔self.f/self.inner〕を保つ＝`concatParams`/`concatArgs`。**match arm body は outer env で emit**＝`setEnumEnv` を bindMatchPayload だけに限定〔同じ clobber 系〕。**placePtrOf が Field place 対応**＝`self.inner.next()`〔inout self〕が copy でなく base local storage への GEP〔in-place mutate・無限ループ修正〕。**exprStructIdx/exprTypeRef が generic field 型を ground**＝`inner: I`→Counter〔struct dispatch〕/MapIter[…]〔chain の generic-receiver dispatch〕。**llvmScalarTy が generic instance 束縛の型パラを llvmTypeOfRef で解決**〔bare name 再帰で args を落として i64 化していた〕・registerGenStructs を 2 pass〔全 named type 作成→body 設定〕で相互参照 adapter を順序非依存に。emitIterNextLoop が要素 kind を ground〔map→Bool は i1＝print で zero-ext・-1 でなく〕。**Iterator 系は全 green**。
- **済 追加（v52-v56・pass=115→119）＝generic コンテナ × array**：v52 array-typed generic enum/struct payload〔`Maybe[Array[I32]]`＝fieldWords/llvmScalarTy/buildEnumValue/bindMatchPayload を type-param→Array に対応・arrayFieldElemRef〕／v53 非 generic enum の array field〔`Word{Tagged(tags:Array[I32])}`＝enumAllPrimitive が array を許可・2 word・variantPayloadTy が arrayTy〕／v54 generic struct instance の array field〔`Holder[I64]{items:Array[Box[T]]}`＝exprStructIdx/exprResultRef が generic Make を解決・arrStoragePtr が base local の LLVM 型で GEP・arrElemTyOf が base の instance env で element 型を ground・exprStructIdx に Index case〕／v55 `arr.get(i:).field`〔array-index sugar も element struct 解決・arrFieldElemStructIdx を Index/Method-get で共有〕／v56 **locElemStructIdx 追加（CoW 用の dormant infra）**。**注意＝array-of-struct LOCAL の `store[i].field` は CoW 未実装ゆえ意図的に未解決**（有効化すると aliased `b=a; b[i]=x` が共有 buffer を壊す latent miscompile＝cow_nested/array_elem_inout_method が loud fail した。soundness 優先で skip 維持）。
- **済 追加（v57-v60・pass=119→129）＝値意味論 CoW（本丸）達成**（tag `llvm-cow-value-semantics`）：eager value-semantics コピーを「place→新 owner」移動の各所に挿入＝leaky な buffer 共有が観測上正しい値意味論に。`copyValueIfPlace`〔array は `copyArrayValue`〔runtime `plew_arr_copy`〕・struct は `deepCopyStructValue`〔array field を deep copy・nested struct を再帰・boxed/Ref は共有のまま〕。挿入点＝array/struct の Let bind・代入 `b=a`・return `return b.xs`・struct field 構築 `<Holder items=a/>`〕。`arrayExprIsPlace`/`structExprIsPlace`〔Ident/Field のみコピー・literal/call/append 結果は既に owned〕。**CoW で aliasing が無害化したので array-of-struct LOCAL アクセスを解禁**＝`arrFieldElemStructIdx` の Ident case 復活（locElemStructIdx 消費）＝cow_nested/cow_struct_array_elem。**`placePtrOf` に Index case**＝`xs[i].bump()`〔inout method through array element＝get-modify-set〕が owned buffer の element ptr を返し in-place mutate＝array_elem_inout_method。**nested place assignment `arr[i].field=v`/`+=`**＝placePtrOf〔Index/Field 一般〕＋`structLlvmTyOfExpr`〔generic instance 型 or 非 generic registered〕で base struct ptr を GEP＝array_elem_field_set/compound。**cow-gap=0**〔cow_array/cow_struct/value_semantics_more/return/struct 全 green〕。残＝深い deep-copy〔array/Ref を含む要素の deep copy・cyclic graph〕は ARC refine 時。
- **済 追加（v61-v65・pass=130→136）＝generic/Ref/nested array アクセス**：v61 generic array-typed field の値意味論〔`Box[Array[I32]]{v:T}`＝`fieldArrayElemRefUnderBase`〔field 型を base instance env で ground して array element ref〕を exprIsArray/arrElemTyOf/deepCopyStructValue に・struct Let bind を copyValueIfPlace 経由に〕＝value_semantics_generic／v62 nested generic field access〔`bb.v.v`＝exprStructIdx/exprTypeRef が field 型を BASE の instance env で ground＝generic_struct_multi〕／v63 array の Ref/compound 要素〔`Array[Ref[Box]]`＝`locElemTypeRef`〔array local の element TypeRef〕追跡＋exprTypeRef Index case・array element LLVM 型を element ref の llvmTypeOfRef から〔mangled span は i64 化していた〕＝arrow_store_literal/array_ref_arc〕／v64 generic/Ref 要素を field/generic local 経由〔`Array[Box[I32]]` boxes[0].v・`hc.items[0]->v`＝arrFieldElemStructIdx Ident generic・exprTypeRef Index Field base＝array_compound_elem/array_generic_field〕／v65 **nested array `Array[Array[T]]`**〔`m[i][j]`・depth-3・`m.append([1,2,3])`＝exprTypeRef Index 再帰〔nested は base element ref から 1 Array 剥がす〕・exprIsArray/arrElemTyOf Index case・genLlvmAppend が array-literal 要素を recv の inner element 型で構築＝array_nested〕。
- **済 追加（v66・pass=136→138）＝unique struct の stack deinit**：scope-exit deinit〔spec/03〕＝structRegisterable が unique を許可〔layout 同一〕・合成 `deinit` method を entry-module method として emit・`dropScope`/`emitDeinitCall`〔unique local の deinit を load した self で・reverse 宣言順〕を block fall-through と return で・`structDeinitFi`〔deinit method 検索〕・`curBodyMark`〔param skip＝borrow/inout/by-value self は caller 所有〕・返却 local は exclude〔moved〕。**深い move/Ref パターンは loud reject〔skip・miscompile 回避〕**＝`move fn`/`move` param〔deinit 型〕・`Ref[deinit pointee]`〔review-items に記録〕。unique_deinit/unique_inout green。
- **済 追加（v67-v68・pass=138→145）＝unique/deinit/move/Ref 完全達成**：v67 move tracking〔`locOwned`〔body local＋move param＋move-fn self が owned・inout/borrow/by-value self は caller 所有〕/`locMoved`〔move arg・move-fn receiver・`move x`〔Expr.Move〕で印〕・dropScope/emitDeinitCall が `locOwned && !locMoved` でゲート・fall-through で owned param も drop・**再帰 deinit**〔`emitDeinitValue`＝自身の deinit→フィールドを宣言順・`structNeedsDeinit`〕・**再代入で旧値 deinit**〕／v68 **Ref refcount**〔`plew_rawbuf_retain`/`release`〔cell header h[-1]〕・Ref share〔`val r2=r`〕で retain・scope-exit で release→0 で pointee 破棄・`refDeinitPointeeSidx`〕。**unique cluster 全 9 本 green**〔deinit/inout/move_fn/cond_return/move/nested/move_into_field/reassign/ref〕。
- **済 追加（v69-v70・pass=145→147）**：v69 **String method dispatch**〔`s.isEmpty()`＝String receiver を std method へ・ensureProvidedDeclared が String lang-item〔strty〕も許可・self を receiver 型の kind で bind〔isStringBytesField が self.bytes を認識〕〕＝string_methods／v70 **array-returning call の binding 推論**〔`val a=mk()`＝exprIsArray/arrElemTyOf に Call case〕＋**array-literal return**〔`return [10,20,30]`〔→Array[I32]〕を宣言 element 型で構築・i64 default の stride 破壊を修正〕＝let_infer。
- **済 追加（v71-v75・pass=147→154）**：v71 **closure mut/shared-cell capture**〔`mut val` を heap cell に〔locPtr＝cell・enclosing/closure 両方が同一 cell・makeCounter で count が escape〕・env に cell ptr・body は cell ptr に直 bind・closureSupported が scalar boxed を許可・valueKind に Call case〔closure-typed local call の戻り kind＝String を sext しない〕＝closure_mut_capture/closure_shadow_capture〕／v72 **generic free fn〔Array[T] param〕の T 推論**〔llvmInferFnArgs が c.locals 非依存の arrayExprElemRef を使う＝array_generic_fn〕／v73 **closure arg からの type param 推論**〔llvmArgTypeRef が closure literal の fn TypeRef を構築・unifyTypeParam が return/param 位置の B を一致＝generic_struct_infer〕／v74 **readStdin() intrinsic**〔runtime plew_readStdin＝String 返す・stdin_count〕／v75 **RawBuffer floor intrinsics**〔rawAlloc/rawLoad/rawStore/rawCap/rawIsUnique＝rawbuf runtime に・RawBuffer[T] は opaque ptr・local が RawBuffer[T] ref を track して element 型復元＝raw_buffer/raw_struct_cow〕。
- **済 追加（v76-v78・pass=154→157）**：v76 **primitive Format method＋lazy std free fn**〔`(42I64).format(...)`＝primLangItemRecvName で int literal/cast/String の receiver dispatch・ensureProvidedDeclared が prim int receiver も許可・`stringFromBytes` intrinsic〔Array[U8]→String〕・`ensureFreeFnDeclared`〔std free fn〔digitByteI〕を到達時に遅延 emit〕＝format_int〕／v77 **trait provided assoc fn**〔`Greeter.banner()`＝ensureFreeFnDeclared が assoc fn〔recv 型あり self なし〕も emit＝trait_assoc_fn〕／v78 **user method on Array[T]**〔`impl[T] Array[T]{first()}`＝Array[elem] で mono・self を isArr で bind〔self.get/self.count が builtin array へ〕＝array_methods〕。
- **済 追加（v79・pass=157→158）＝extern(c) FFI**：foreign fn を raw C symbol で call〔ffiTyOf＝prim int/String/Bool はそのまま・opaque `type`/CPtr[T] は ptr・宣言は nameId を runtime cache の小 id〔1-30〕の先へ offset して dedup〕・cString/stringFromCString/arrayCPtr intrinsic〔runtime plew_cString/plew_stringFromCString〕・llvmScalarTy が CPtr[T]→ptr・**test-llvm.sh が companion `tests/run/<name>.c` を link**〔test.sh と同じ〕＝ffi_extern_c。
- **済 追加（v80-v81・pass=158→160）**：v80 **boxed 再帰 field 代入＋値意味論**〔`a.next=<Optional.None/>`〔boxed field〕＝expected 型で構築〔bare construction 推論〕→fresh heap cell 確保→repoint・copy `b=a` が旧 cell を指したまま不変＝rec_value_semantics〕／v81 **method-own type param `[S]`＋where bound〔keystone〕**〔`absorb[S](sink: inout S) where S: Sink`＝`genMethodOwnTypeParamCall`＝method type args を call から infer〔llvmInferFnArgs〕・**pseudo instance ref**〔receiver name＋infer した args〕で mono key〔body は curTypeParams=mf.typeParams→curTypeArgs=margs で emit・self 型は receiver name から〕・struct/primitive receiver 両方・primLangItemRecvName が prim-typed field 認識・genLlvmFuncBody が pseudo instance〔head 非 generic〕の receiver-name struct を保持〔self.a 解決〕・generic `inout S` param が method env で具体 struct〔Bag〕に ground〔sink.put dispatch〕＝generic_method_bound_inout〕。
- **済 追加（v82-v83・pass=160→164）＝Hash/Hasher/SipHash/Dictionary 完全達成**：v82 **Hash/Hasher/SipHash**〔bindOneParam が generic param の型を ground〔`key:K`〔K→String〕で String method dispatch・`v:K`/`v:U64` は primitive 型名を locTypeRef に track〕・primLangItemRecvName が primitive-int Ident local〔`v.hash[H]`〕を解決・struct method path が**非 generic method を全て遅延 emit**〔SipHasher.writeU64/round/finish を transitive 到達時に declare+emit〕＝hash_siphasher/generic_method_transitive_hash〕／v83 **Dictionary lang item**〔`[k:v]` literal＋`d[k]`→`d.at(key:)`・`d[k]=v`→`d.insert(key:,value:)`〔spec/02・user Index/IndexSet より先に special-case〕・generic Dictionary method〔at/insert/find/grow＋where bound〕は generic-method path で・valueKind Index が env-aware arrElemTyOf 使用〔generic array field 要素 `self.keys:Array[K]`〔K→String〕を String 認識→`self.keys[i]==key` が String byte-compare〕＝dictionary_literal/subscript〕。
- **🎉 済（v84・pass=164→168・skip=4→0）＝async サブシステム完了＝LLVM backend がテストスイート全体を pass（C backend と機能パリティ達成）**：**async state machine を LLVM IR で full 実装**〔async_basic/control/match/order〕。設計＝C backend `Codegen/Async.pw`〔方式 B・stackless〕の LLVM 版。
  - **runtime は C を流用**（Plew でも手書き IR でもなく）：`emitLlvmRuntime` に async runtime〔PlewPromise/FIFO ready queue/virtual-clock timer/event loop/sleep〕を C 文字列で追加。**C backend と同一コード**だが、IR が呼ぶシンボル〔`plew_promise_new`/`plew_promise_resolve`/`plew_sleep`/`plew_loop_run`/`plew_frame_alloc`/`plew_frame_free`〕は**external linkage**（IR は別 TU ゆえ `static` だと未解決）・queue/timer helper は file-local static。`PlewPromise` の C レイアウト＝IR の `{i32 done,i64 value,ptr k,ptr kframe}` と一致。
  - **state machine は IR で**：`async fn`→frame struct〔named LLVM struct `{i32 state,ptr self,ptr sub,<params>,<hoisted locals>}`〕＋resume fn〔`void(void*)`・entry block で全 frame-field GEP を precompute〔dispatch switch reachable な resume block を dominate させる＝"does not dominate all uses" 回避〕＋`switch __state`〔await 点ごとに `case state→resume block`〕〕＋entry fn〔frame 確保→param コピー→`self=promise_new`〔resume 前に capture＝同期完了で frame free されても dangling しない〕→resume 1 回→非main は self 返却 / main は `loop_run`→ret 0〕。
  - **await=suspend point**（`emitLlvmAsyncSuspend`）：`__f->__sub=操作対象 promise`・`__f->__state=N`・`!done` なら `__sub->k=resume fn`/`__sub->kframe=frame` 登録して `ret void`、`done` なら resume block へ fall-through。resume block を switch case に追加。await 値は `__f->__sub->value`〔i64 型消去・`asyncSubValue` は frame から sub を reload＝switch edge で SSA が dominate しないため〕。
  - **frame field 配線**：`let`/match-bind の storage は alloca でなく frame field GEP〔suspend を跨いで生存〕＝`asyncFrameSlot`・`collectAsyncFieldTys`〔pre-pass〕と body 走査が同一 DFS で field index 一致。`return`→`self` を resolve〔`return await e` は先に suspend〕→frame free→`ret void`。`isPlainUserFn` は async 除外・`declareAsyncFns`/`emitAsyncFns` が driver。
  - **制限（additive）**：generic async / async method は loud reject。promise 値は i64 型消去（scalar/() のみ・struct/String 結果は後）。`@Std/Async.sleep` intrinsic→`plew_sleep`。
- **これで LLVM backend は `./test-llvm.sh` pass=168 skip=0 fail=0＝全テスト pass。C backend（292＋不動点）と機能パリティ。** 残る差分は additive（generic async・struct promise 値・FFI 拡張等）。
- **➡ 次の目標＝LLVM 上での self-host**（`plewc-llvm` がコンパイラ自身 `compiler/src/_.pw` をコンパイルできる状態）。進め方＝`compiler/plewc-llvm compiler/src/_.pw` を回し、fail-fast の各 gap を潰す（巨大コンパイラ相手の grind）。
  - **済（v85・pass=168 維持）＝コンパイラ全体が LLVM backend で「コンパイル＋リンク」可能に到達**（実行時の正しさはまだ＝下記）。潰した gap：
    - **enum 型フィールドを持つ struct**（`Tok{kind: Kind}`）＝`structRegisterable`/`enumRepresentable` を純粋な型グラフ述語に（登録順非依存・非 boxed 値サイクルは front-end エラーゆえ停止）・登録を3相に分割〔struct 名→enum 全体→struct 本体〕で相互参照解決（`registerStructNames`/`registerStructBodies`）。
    - **array param/match-bind の要素 struct 追跡**（`lhsNames: Array[Bind]` の `lhsNames[i].field`・`Expr.Call(.., val args)` の `args[i].field`）＝`bindOneParam`/`bindMatchPayload` で `setLocalElemStruct`/`setLocalElemTypeRef`。
    - **`&&`/`||` 短絡**（値・条件両用・i1 を PHI 合流）＝`genLlvmShortCircuit`。
    - **`break`/`continue`**＝loop ターゲットスタック（`loopBreakBB`/`loopContBB`/`loopDepth`・overwrite-in-place）＋ for ループに step ブロック（continue が増分を飛ばさない）。
    - **enum 構築の省略フィールド**（後付け field `isNeg` を省く `<Expr.Int … />`）＝payload を ZERO 開始（undef でなく）し、省略フィールドは default かゼロ（C designated-init と同形・**positional fallback を撤去**＝named 構築のみ）。
    - **process/file intrinsic**（`argCount`/`argAt`/`writeByte`/`readFile`/`readFileBytes`/`fileExists`）＝`(ptr,i64)` ABI でランタイム関数化（struct-by-value ABI を仮定しない）・`main` を `i32 main(i32,ptr)` 化し `plew_set_args` で argc/argv を runtime へ。
  - **済（v85 続・self-host 検証）＝LLVM-built compiler は小プログラムで C backend とバイト一致**：`compiler/plewc-llvm compiler/src/_.pw`→IR→リンク→`/tmp/pvl`（LLVM-built compiler）が **`tests/run/*.pw` の 148/168 を canonical plewc とバイト同一の C を出力**（differ=4・crash=16）。※初期の "arith reject" は**ミスコンパイルでなく `/tmp/std` が 6/6 の stale コピー**だった（`/tmp/pvl` は argv[0] 基準で stdRoot=`/tmp/std` を読む＝`cp -r compiler/std /tmp/std` で同期すれば一致）。**self-host 検証時は必ず /tmp/std を同期**。
  - **🎉🎉 達成（v86・LLVM SELF-HOST）＝`plewc-llvm` でコンパイルしたコンパイラがコンパイラ自身を不動点で再生産**。`plewc-llvm compiler/src/_.pw`→IR→リンク→`/tmp/pvl`（LLVM-built compiler）が **`compiler/src/_.pw` を canonical plewc とバイト同一の C〔1,217,629 B〕を出力**・**全 168 テストもバイト同一**・**gen2 不動点も一致**（`/tmp/pvl` の出力を clang→再度 `_.pw` をコンパイル＝gen1==gen2）。潰した 2 つの根本バグ：
    - **inline enum payload フィールドのサイズ過小**（`fieldWords` が enum 型フィールドを **1 word** と数えていた＝実際は `{i64 tag,[maxWords] payload}`＝`1+maxWords` words）。enum/struct-with-enum を payload に持つ enum の `[N x i64]` payload 域が小さすぎ、大 variant 構築で**オーバーフロー→隣接 stack/heap を破壊**（heap 状態依存の miscompile＝小テストはほぼ素通り・コンパイラで露呈）。修正＝`fieldWords` が inline enum を `1 + enumPayloadWords(eid)`（非 boxed 値サイクルは front-end エラーゆえ停止・boxed=1）。**これで test 148→168 全バイト同一（crash 16・differ 4 が全消滅）**。
    - **loop 内 alloca の stack 蓄積**（LLVM backend は builder 位置で `alloca` を吐く＝loop body 内だと反復ごとに stack が積もる・C backend/clang は entry へ hoist）。大入力で 8MB stack を溢れて SEGV（`_.pw`・`/tmp/pvl tests/run/*` の一部）。**最小再現＝2M 反復で毎回 enum 構築→`brk`**。修正＝**全 alloca を entry ブロックへ**（`beginFnBody`＝`entry`→`br body0`→`body0` で安定アンカー・`entryAlloca`＝`LLVMGetEntryBasicBlock(curFn)` の terminator 直前へ挿入して builder 復帰）。`genLlvmFuncBody`/`genLlvmMain`/`emitClosures` は `beginFnBody`、async resume は switch 終端の entry を流用。**26 alloca サイト→`entryAlloca`**。
  - **これで LLVM backend は (a) 全 168 テスト pass (b) コンパイラ自身を不動点で self-host＝C backend と完全パリティ＋自己ホスト能力。** 残る additive（generic async・struct promise 値・FFI 拡張）は両 backend 共通の未実装。**self-host 検証手順**：`./build-llvm.sh`→`cp -r compiler/std /tmp/std`→`compiler/plewc-llvm compiler/src/_.pw > self.ll`→`clang self.ll <(plewc-llvm --runtime) $(llvm-config --ldflags) -o pvl`→`pvl compiler/src/_.pw` を `compiler/plewc compiler/src/_.pw` と `cmp`。
  - **C 完全削除→LLVM 一本化に着手（方針転換・ユーザー決定）**。背景＝C backend は分離可能な大塊でなく、`_llvm.pw` は `_.pw` と同じ Codegen part（`Emit` 含む）を共有し LLVM root が `Backend/Llvm` を足して `emitLlvm` を呼ぶだけ（純 C 専用＝`Codegen/Emit.pw` 674行＋`Driver` の emitC/C-runtime 部のみ・残 24k は共有フロント）。削除で失うのは①clang のみブートストラップ（種 `plewc.seed.c`＝emitC 産）②独立した正解オラクル（LLVM を C 出力にバイト一致で検証）③メタプロ `--gen`（C 専用だった）。**段取り＝(1) メタプロ移植〔済〕→ (2) ブートストラップ作り替え〔重・要決定〕→ (3) part/reject 等テスト移植→ (4) emitC・_.pw 削除・bootstrap.sh/test.sh 改修**。
    - **(2-準備) LLVM backend が `_llvm.pw` 自身を valid IR に self-compile＝達成（v90）**。これまで `compiler/plewc-llvm` は**ずっと C backend 経由でビルド**され、自分の IR で `_llvm.pw`（libLLVM FFI を多用）をビルドしたことが無かった＝IR 種ブートストラップの前提（LLVM が LLVM root を正しくコンパイル）が未開拓だった。露呈したバグは1点：**`extern(c)` 不透明ハンドル型（`type LLVMTypeRef`＝`StructDef.isCExtern`・C backend は `typedef void*`）を `llvmScalarTy` が i64 既定に落としていた**（FFI call 経路の `ffiTyOf` は ptr に落とすが、user 関数の戻り/引数/フィールド型を算定する `llvmScalarTy` は未対応）→ `LLVMTypeRef` を返す user 関数（例 struct 型を組む `pf648`）が `define i64` 宣言なのに `ret ptr` で **invalid IR**。修正＝`llvmScalarTy` の i64 既定手前で `isCExtern` 構造体を `ptr` に。**これだけで `_llvm.pw` の IR が valid＝llvm-as 検証 pass、種ビルドした `plewc-llvm0` が arith 等を正しくコンパイルし、自分の IR を種とバイト一致再生産＝IR 不動点**。検証の罠＝`computeStdRoot` はバイナリの dir に `std/` を付すので **plewc-llvm0 は std の1つ上**（`tmp/bs/plewc-llvm0`＋`tmp/bs/std/`）に置く。回帰なし（run168/panic11/gen10/reject1・`_.pw` self-host byte 同一・C backend 292）。
    - **(1) メタプロ `plew gen` を LLVM へ移植＝済（v89）**。`--gen` は `runFrontend`（共有）が harness AST をアリーナに合成して通常 emit に流す仕組みで、`emitLlvm` まで既に届いていた。唯一の欠落＝**gen モードでユーザー自身の `main`（マクロ生成関数を呼ぶ＝この build に未存在）を抑制していなかった**（C backend は Driver 628-665 で `genMainIdx` 以外の main をスキップ）。修正＝LLVM の fn emission ループ（Llvm.pw 8097-）で `c.genMode` 時は `genMainIdx` だけを main 扱いし dead user main を完全スキップ。これだけで **gen 10/genreject 1 全 pass**（harness 内の `parseItem`/`deriveFromSource`/@Std/Syntax 経路は self-host 済ゆえそのまま emit 可）。**恒久ハーネス `test-gen-llvm.sh` を新設**（C backend test.sh の gen/genreject に対応＝harness IR→link→run→`.gen.pw`→App auto-part→照合・scratch は `tmp/` gitignore・cleanup 内包）。
  - **v88＝checked 整数演算で C backend と観測パリティ完成（overflow / 0除算 / assert message）**。self-host 後に判明した唯一の観測差＝**LLVM backend が overflow/0除算を panic せず wrap/UB していた**（run/168 は正常系ゆえ素通り・`tests/panic/` は test-llvm.sh 未収録だった）。修正＝**checked 算術床**：add/sub/mul/div/rem/neg を生 IR op でなく runtime helper `plew_<w><Op>`（i8-i64 符号別 8 prefix・本体は C backend の `emitIntRuntime` と同一＝`__builtin_*_overflow`＋div-zero/INT_MIN ガード・external linkage で別 TU の IR から呼ぶ）経由に。配線＝`buildBinOpChecked`/`buildNegChecked`（幅は `LLVMTypeOf` から・符号は witness は `uns`／compound は `valueKind(target)==3`・field は `isUnsignedName(fld)`）を **witness 床(genPrimOpWitness)・直接 binary・primitive unary neg・compound 代入5箇所**へ。`buildBinOpS` は wrapping 床として温存（`wrapping*` 専用）。**assert(cond, message:)** の custom message も配線（従来は "assertion failed" 固定）。`test-llvm.sh` に `tests/panic/` 11本を恒久収録（overflow×7/div-zero/array-elem-field/assert×2）。**self-host バイト同一（1,217,629 B）＋不動点は不変**（コンパイラ自身は overflow しないので helper は発火せず出力不変）・C backend 292 も不変（`Backend/Llvm.pw` は `_llvm.pw` のみが part）。これで line 96 (i) overflow→panic は **済**。
- **次の段（この順）**：(a) **値意味論 ARC＋CoW**（cow-gap 6本・最重量・言語の核＝`c.boxedFields`〔`fieldIsBoxed`〕で recursive enum/struct field を refcount heap cell に・C backend は `emitBoxedFieldRetain`/`emitBoxedFieldRelease`〔Decl.pw 226-〕参照・これが recursive enum 構築・unique deinit・cow-gap・Ref refcount を一括で解く本丸＝v27 の leaky Ref を refcount 化する所と地続き）。**payload enum Ord**（per-variant switch・derive_ord_enum）。(b) recursive/generic enum 構築（List/Tree・iter_chain＝boxed payload に依存）。(c) **print[T]/Format**（struct/enum print・generic print instance）。(d) closure（lambda lift）/async（state machine）。(e) Optional 系（`?.`/`try`／OptChain/Try）。(f) nested array `Array[Array[T]]`（`m[i][j]`＝element-of-element 型追跡が要・現状 locElemTy は arrayTy で内側型ロスト）・user Array メソッド（emitArrayMethods 相当）。(g) payload enum ==/Ord（per-variant switch）。(h) raw memory（`rawAlloc`/RawBuffer 直）・stdin。(i) overflow→panic。**call-graph 到達可能性**でエントリモジュール限定置換（assoc は v23 で `scanExprAssoc`/`scanStmtAssoc`/`scanBlockAssoc` walker 導入済＝free fn 全般へ拡張可）。各段 `./test-llvm.sh` で回帰ガード（pass=77）。**残 FFI**（`repr(c) struct`・`CInt`/`CSize`・所有権ラッパ・`.toOptional()`/`load()`）は別軸 additive。
- **追加の罠（Backend/Llvm.pw）**：⑧array は `{ptr,i64}` で element-size generic runtime（typed GEP で per-elem・`LLVMSizeOf`・CoW なし＝buffer 共有）。配列リテラルは elemTy をリテラルからでなく**束縛/param の annotation から**取る（i64 デフォルト混入を防ぐ）。⑨LLVMValueRef の null sentinel は kind では判別不可＝`LLVMIsNull` を使う（place ポインタ有無判定）。⑩aggregate（struct/enum/array）に icmp/extractvalue を**非 aggregate へ誤適用すると LLVM builder が SEGV**＝必ず `LLVMGetTypeKind==10/exprIsArray` でガード。⑪inout param/self は alloca せず**ポインタ param を locPtr 直結**。⑫`coerceInt` は整数のみ（非整数は素通し）＝array 値に呼ぶと不正 cast。
- **LLVM backend の罠（Backend/Llvm.pw のヘッダ＋コメントに詳細）**：①ユーザー関数/メソッドは合成名 `pf<idx>`＋index→キャッシュ `LLVMValueRef` でディスパッチ（マングル不要）。②narrowing `as` 不在ゆえ LLVM API の count/index/aggregate-index は**バインディングで U64 宣言**（LP64 で安全）。③`c.locals` 非投入なので **env に locKind/locStructIdx を持たせて型判定**（typeOf は局所に効かない）。④match の payload bind は**フィールド名で punning**＝`val x` は「フィールド x を束縛」・別名は `field: val local`（`val fbase` で非存在フィールドを束縛しエラーになりやすい）。⑤メソッド emission をエントリモジュール限定にしないと std メソッド本体（未対応構文だらけ）を引き込んで死ぬ。⑥primitive lang-item（I64/String 等）は **extern struct として c.structs に居る**ので「nested struct field」判定で extern を除外。⑦`self` は "self" バイト span を c.bytes 末尾に append して束縛。
- **FFI 設計（使う側のみ・spec/15）/最小サブセット/ラベルなし/first light は下記に保持（既存実装の正典）**。

**➡ 次の本命候補（次の大物）**：
1. **Iterator 拡充**＝終端（reduce/fold/count/sum/collect/any/all/first）＋遅延 adapter（take/zip/enumerate/skip・`MapIter`/`FilterIter` をテンプレに）。demand-driven 単相化が動くので確立パターンに沿うだけ。
2. **並行 additive**＝`spawn`/`JoinHandle`/チャネル・spawn 境界の意味論強制（move/copy のみ越境・`Ref` は spawn 不可）・ランタイム C 生成・`local` 伝染解析。重い（言語仕様の決定が出る）。
3. **`any P` 存在型**＝動的ディスパッチ（vtable 相当・メンバ単位診断・異種混在）。重い・別軸。
4. **循環回収**＝Ref グラフ限定サイクルコレクタ（Bacon–Rajan trial deletion）。設計確定済（[design-decisions.md](design-decisions.md) 39-49）・優先度中。

**M3＝パッケージ管理導入後に `@Std/Syntax` を in-tree から外部共有パッケージへ昇格**（コンパイラもマクロも同一版に依存＝Rust の 2 パーサ版違い問題回避）。

## Dictionary[K,V]（lang item・完動）

**設計**：全て Array 上に構築（平行配列＝CoW/値意味論は Array から継承）。`compiler/std/Prelude.pw` 末尾＝`struct Dictionary[K,V] { mut val keys: Array[K]  mut val vals: Array[V]  mut val hashes: Array[U64]  mut val buckets: Array[U64]  pub(get) mut val count: U64 }`。open addressing・線形プロービング（`buckets[slot]`＝エントリ index+1・0=空／密 append／空＋初回 insert で grow cap0→8／負荷 0.75 で倍化 rehash）。メソッド（`pub impl[K,V]` 1 ブロック）＝`hashOf`/`grow`/`insert`/`find`/`contains`/`getOr`/`at`（欠落 panic）。`get->Optional[V]` は Optional ambient 化後。**添字**＝`dict[k]`→`.at`／`dict[k]=v`→`.insert`（codegen が合成 Method・`isDictionaryType`/`synthDictMethod`）。**リテラル**＝`[k:v]`/`[:]`（パース＝ParseBody の `Kind.LBracket` 分岐・lower＝下記「give-block 脱糖」）。test run/dictionary_subscript・dictionary_literal。

> **🔴 必須 TODO（忘れない）＝ランダムシード（RandomState 相当）**。Rust の `HashMap` は SipHash-1-3 ＋ **マップごとにランダムキー**（DoS 耐性）。Plew にはまだ RNG（`Random`）が無いので**当面は固定キー（決定的）**で実装済。**`Random`/RNG 着手後にマップごとのランダムシードへ差し替える**（アルゴリズム・トレイト形は不変・シード源を差すだけ＝additive）。固定キー間は DoS 耐性なし（内部マップには無害・反復順は spec 未規定）。

メタプロの段取り・実行系の具体は [metaprogramming-architecture.md](metaprogramming-architecture.md)。

## 並行・後続（renovate の残り・ロードマップ）

- **Phase B 残り（要 supervised）**：checks（10 箇所の `exprType`）を typed-AST に統合＝3 重 walker 一本化／pre-fill パスで codegen を完全 pure-read／`exprType` 再導出を駆逐。per-function clear で grounded をキャッシュできたので**テンプレート型は不要**になった。
- **Phase C＝名前 interning（宣言名）→ Phase D＝型 interning＝✅済**：宣言名スキャン＋全型・型パラメータ照合を byte 比較から整数比較へ（src `spansEqual` 143→16）。実装＝下記「名前/型 interning」。残る 16 spansEqual は use-site 名（match-arm variant・capture・import・MakeField・arrayElem・curRecv・DeriveReq）＝型の範囲外で利益限界的・据え置き。**「Dictionary を dogfood」は不採用**＝span キーの interner は `Dictionary[String,U64]` だと intern ごとに String アロケート（span→String スライス無し）でホットパスに不適＝手書き open-addressing span テーブルが正解（dogfood は別途 id キーの場所で）。
- **kwSpan/rangeEquals は据え置き（評価済・id 化せず）**：`kwSpan`＝byte を生む keyword span（emission が消費＝id に逆写像なし）。`rangeEquals`（span を keyword リテラル "Array"/"Ref"/… と比較・~40 が TypeRef 名）も id 化しない＝短 keyword への byte 比較は早期 exit で既に安価、生 span への per-call intern は同コスト（中立）、keyword id テーブルも ~同コストで明確な高速化にならない（churn と 0-id footgun リスクの割に利益薄）。spansEqual（任意 2 span の O(len) byte 比較を N-scan で回す）の id 化が真の利益で、それは Phase C/D で達成済。
- **重い D'（後日・本物の refactor）**：Phase D は型 span に id を**載せて照合を整数化**した段で、legacy 型 triple `(start,len,isArray)+ref` の**構造的 ref 一本化**（triple そのものの撤去）は別物・未着手。derive 合成 ~280 行が `Parser/Decl.pw`（＝メタプロ dogfood で置換されるので触らない）。
- **横断 additive**：本命（Iterator 拡充・並行 spawn・`any P`・循環回収）は上の「次の一歩」に詳細。演算子トレイト配線は一段落（残は小・additive＝上記）。詳細は [provisional.md](provisional.md)。

## 再利用資産・罠（git で拾いにくい知見）

- **型付き AST キャッシュ**（現アーキ）：`typeOf(c, id)`＝codegen の型読み口（旧 `exprType` 再導出の代替）。ノード型を `Comp` の **4 並列 scalar 配列**にキャッシュ（self-host は **struct 配列 IndexSet 非対応**ゆえ・`arr[i]=v` の scalar IndexSet は可）。**body emission のエントリ（`genFunc`/`genClosure`/`genAsyncFunc`）で `clearExprTyCache`**＝env（Self/型 args/インスタンス）は 1 body の emission 中だけ一定なので、grounded 型をその間だけキャッシュ・関数境界で破棄（全文脈で安全・filled-id リストのみ reset で O(fills)）。`exprType` は今や `typeOf` 初回 touch／自身の再帰／pre-codegen checks からのみ到達。
- **共有パーサ→arena lower（A）**：共有 @Std/Syntax パーサが値ツリー（`TopItemAst`/`DeclAst`/`ImplAst`/`TraitAst`/`ExternAst`/`ImportAst`/`DirectiveAst`／式文は `ExprAst`/`StmtAst`…）を返し、`Codegen/Lower.pw` が arena（`Expr`/`Stmt`/`Func`/`StructDef`…）へ lower。**名前の offset 規律**＝`moduleOf` に効く名前（free fn 宣言名・call 名・extern fn 名・import/export 名）は**原本 offset 維持**（再インターン不可）、それ以外（struct/enum/field/型名・impl 系全部）は `internBytes` で再インターン（codegen は text 出力ゆえ byte 同一・moduleOf 不参加）。`lowerType` が `recordArrayElem` で `arrayElems` 種まき（旧パーサの codegen 仕事は lower 側へ）。**罠**：パース駆動ローディングで `c.bytes` はファイル源と再インターン名が交互配置になる（各ファイルを追記直後に lower）。writeSpan は text 出力・moduleOf は per-file レンジ参照ゆえ不動点・可視性とも安全（offset 値そのものに依存するロジックは無い）。
- **block 0 は dummy 予約**：`body` フィールド 0 は「無本体」sentinel（extern fn・trait 要求シグネチャ）。entry ファイルは ambient Prelude より先に lower されるので、予約しないと最初の関数本体が block 0 に来て無本体と誤判定される（`body != 0` で gate する body-walk 全部が誤 skip）。`_.pw` の Comp 初期化で `c.blocks.append(<Block stmts=[] />)`。
- **再帰値型 box**：`markBoxedFields`（`Mono.pw`・`Comp.boxedFields`＝field の `nameStart` キー）が循環を閉じる**ユーザー側フィールド**を box（`Array`/`RawBuffer`/`Ref`/`WeakRef` は cut・`Optional`/`Result` は透過して引数を辿る＝Optional 自体は box せず hardcoded `.data.Some.v` を温存）。`T*`・構築 `genBoxCell`（`curRetTy=fd.ty` で nested JSX を mangle 推論）・読み deref コピー・書き fresh box に repoint（共有セル不変＝値意味論）。box＝`plew_arc_alloc` rc セル・copy/share は retain・release は rc→0 で pointee 再帰 release＋free。**ARC 解放は配線済**だが残リークは print/String の既存ギャップのみ（box 由来 0）。終端なし循環 `struct A{a:A}` は構築不能だが通る（loud reject は将来）。（旧メモ「再帰値型へバリアント追加で auto-boxing/copy が壊れる」は**誤診断＝撤回**。実際の罠は次の 2 つ。）
- **⚠ `Array[Enum]` の要素 deep copy/release**（値意味論 ARC）：`Array[E]` の要素を deep に copy/release するかの判定は `Codegen/Array.pw` の `arrayElemNeedsDeep`＝`structNeedsCopy` **OR** `enumNeedsCopy`。元は `structNeedsCopy` だけ＝enum 要素を取りこぼし、heap 持ち variant の `Array[Enum]`（`Array[ExprAst]` 等）が**浅い要素管理**（buffer だけ copy/free・要素の `<E>_copy`/`_release` 不走）になり、leak＋（append したローカルを drop すると）UAF。**新しく heap を持つ要素種を Array に入れるときはここの判定を必ず確認**。
- **⚠ by-value 引数のローカルはコピーされず格納されうる**：ヒープ持ち値型の**名前付きローカル**を by-value 引数として `append` 等に渡すと、call site で CoW コピーされず**浅く格納**され、scope 末の `release(局所)` で格納要素がダングリングする（一時値 rvalue を渡せば所有権移譲で安全）。パーサ等で「parse 結果を一旦ローカルに束ねてから push」は避け、**`xs.append(parseFoo())` と一時値で渡す**。`Array[Enum]` を deep にした今は前者の罠も顕在化しにくいが、根治（by-value 引数 place の CoW コピー）は未対応＝当面は一時値で回避。
- **値意味論 ARC の在処**：concrete＝`emitFieldAction`（struct/enum 共有・unique-deinit 込み）、mono＝`emitMonoFieldAction`/`emitMonoModeDef`。**heap フィールド種を足すのはこの 2 dispatcher**。need 判定は `fieldNeedsCopy/Release`・`typeInfoNeedsCopy/Release`。
- **give-block 脱糖（codegen で式を組む雛形）**：「empty 構築→副作用文の連打→値を yield」を式位置で出すには arena `IfExpr(cond=Int(1,isBool), thenBlk, elseBlk)`（値位置ブロック＝C statement-expression）を合成し、then で `mut val __t = …; …; give move __t`、else は dead だが型のため同型値を give（Dictionary リテラルの `lowerDictLit`/`synthEmptyDict` がこの形）。**所有権**＝`give move __t` で deep copy なし移譲（return と同じ・`markMovedExpr` が scope-drop を抑止）。素の `give __t` は shallow コピー＋ブロック末 release で UAF になるので不可。**結果型**＝`IfExpr` 値位置は `blockGiveType`（ブロックの `let` を peek 登録→give 式の型→pop）で give-of-block-local を解決（generic inst は `emitMangle`）。
- **missing-return 検査**（`Codegen/Check.pw` `checkReturns`）：非 `()` 関数（`hasRet`・main/async/extern/`body==0` 除外）の本体が全経路で発散しなければ loud エラー。発散＝`blockDiverges`（いずれかの文が発散）／`stmtDiverges`（`return`・`panic`・`if`＋`else` 両枝発散・網羅 `match` 全アーム発散・`while true` で exit `break` 無し＝`blockHasLoopBreak` はネスト loop へ降りない）。Plew は**末尾式を暗黙 return しない**（`give` はブロック式専用）ので必須。spec/01-basics/04-functions.md。
- **generic 呼び出しの引数 T 推論には arg 型に名前 span が要る**：`inferFnArgs`→`tyRefOfInfo` は `nameLen==0` で 0 を返す。リテラル等の `typeOf` が名前なし TypeInfo を返すと T 未推論（`print("hi")` が `print_value_T` 未定義を呼ぶ等）。`Expr.Str` は `stringTypeSpan` で "String" 名 span を付与済。新しいリテラル種を足すなら typeOf に名前 span を持たせること。String は `Core.pw` で `impl String as Format`（`print[T] where T: Format` を満たす）。
- **generic struct field の array 単相化**：`Array[Box[T]]`/`Array[Entry[K,V]]`（compound 要素が外側型パラメータを含む）の field/変数 field は `groundedArrayFieldInfo`（`genericFieldTypeInfo`/`genericEnumFieldTypeInfo`/`substTypeInfo` の array 枝）で instance env 越しに ground＝`ref` 不変で波及ゼロ。template の非 ground compound 要素は runtime/method を emit しない（`skipArrayElem`/`emitArrayMethods` の `tyRefIsGround`）。`arrayElemNameForRef` は env 依存 `sameMangle` で template `T` を concrete に誤マッチしうるので**非 ground エントリを skip**。
- **Iterator/generic 機構**：method-level 型パラメータ（`FnInst.recvInstRef`・`registerMethodInst`・mangle suffix は method-own tail のみ）・**demand-driven 単相化**（`providedRetReachable`＝`FilterIter[FilterIter…]` の無限型族を到達時のみ発行）・Self/Item 解決（`curSelfRef`/`curItemRef`＋`resolveTy`）・構造的型推論（`unifyTypeParam`）・impl レベル `where`（funcBounds へ畳む）。**generic-レシーバ・メソッド本体の推移的インスタンス探索**＝`scanGenInstMethodBodies`（各 genInst のメソッド本体を `emitMonoMethod` と同じ env で scan→nested fnInst 登録・Array レシーバはスキップ）＝`Dictionary[String,U64].hashOf` 内 `key.hash[H]`→`String.hash[SipHasher]` を discover。**罠**：型パラメータ名 vs 具体型名のグローバル衝突（ユーザー struct `B` と `MapIter[…,B]` の `B`）→ `isTypeParamName` は宣言済 struct/enum 名を具体型扱い。
- **generic コンストラクタ型引数推論（JSX）**：`lowerExprWithCtx`（lower 時に context 型を渡し、型引数なし Make の head が一致したら context 型引数を採用＝arena Make を最初から正しい `ty` で構築）＋`Comp.lowerRetTy`（戻り型 context・本体 lower 中だけ set）。
- **名前/型 interning（Phase C＋D）**：`Codegen/Emit.pw` の `intern(c, start, len) -> U64`＝span の**内容**を 1-based id に写す（FNV-1a＋手書き open-addressing・衝突は `spansEqual` 解決）。**0 は返さない**＝`nameId`/`tyNameId` フィールドの既定 0 が「未設定」sentinel。**唯一の content-keyed テーブルを宣言名と型名が共有**＝`TypeRef` が "Foo" を指す id と `StructDef` "Foo" の id が同一なので型 vs 宣言の照合も自然に成立。
  - **Phase C（宣言名）**：宣言テーブル（StructDef/FieldDef/EnumDef/Variant/TraitDef/Func〔`nameId`＋`recvId`〕/Param/Conform〔`typeId`/`traitId`〕/AssocBinding/MethodAlias/Local）に id を持たせ、**lower 時（構築点）に 1 回 intern**。
  - **Phase D（型名・型パラメータ）**：`TypeRef.nameId`（**唯一の choke point＝`pushType` で intern**＝lowerType も Mono の substitution も全 TypeRef がここを通る・直接 `c.types.append` は廃し pushType 経由に統一）／legacy 型 triple head `Param/Local/FieldDef.tyNameId`（`(tyStart,tyLen)` を構築点で intern＝array は element・named は head を指す triple をそのまま写すので `spansEqual(tyStart_a,tyStart_b)` の drop-in・array 癖を考えなくてよい）／型パラメータ・supertrait・assocType 名 `Bind.nameId`（**`internBytes` で一括**＝再インターン名は全部ここを通る）／`FuncBound.paramNameId/traitNameId`。全型・型パラメータ照合を整数 == へ。transient な `TypeInfo` は id フィールドを足さず**比較点で on-the-fly intern**（O(1) 照合ゆえ neutral・~30 構築点を触らずに済む）、`Func.retStart` も同様に照合点で intern。
  - スキャンは「クエリを 1 回 intern→ループは整数 ==」＝N 要素が 1 ハッシュ＋N 整数比較。**query-vs-scan は intern をループ外に hoist**しないと無意味（loop 内 intern は毎回ハッシュ＝byte 比較と同コスト）。
  - **罠**：①構築点を 1 つでも漏らすと id=0 で converted スキャンが取りこぼす silent miscompile（fixpoint＋test で捕捉）。**特に「Bind を返すヘルパ」が raw `<Bind/>` リテラルで nameId を落とすと、その Bind を operand にした比較が `real == 0` で恒偽**＝実例 `assocBindingSpan`（型バインド置換 Bind に nameId 未設定→全 trait 要求が witness されず "incomplete conformance"）。**id を比較する operand は出所が internBytes/pushType/構築点 intern のいずれかであることを必ず確認**。②**Local は動的**（codegen 中に push・move で rebuild）＝`addLocalCn` で intern、rebuild 点（`markMovedLocal`）は id を保つ。③provided-method 複製（`Decl.pw`）は `nameId=f.nameId` コピーだが recv が変わるので `recvId` は再 intern。**フィールドは `val`**＝struct-array IndexSet 非対応ゆえ rebuild-mutation 不可、だから**構築点 intern が正解**。④dev-rebuild は前回 plewc を使うので、変換ミスで壊れたバイナリが次の dev-rebuild を poison しうる＝怪しいときは `./bootstrap.sh`（種から clean）で切り分け。
- **演算子 opcode**：`op` フィールドは `kindCode(Kind)` の整数。`Ops.pw` の `opAdd()..opShrAssign()`（`kindCode(<Kind.X/>)`）が**単一の symbolic 源**（magic 数を codegen に漏らさない）。
- **⚠ ambient Core に「よくある名前」を入れると衝突**（O6 で演算子トレイト `Add`/`Sub`/… ＋ assoc `add`/… を Core に入れた時に表面化・2 箇所修正）：①**bare 呼び出しは assoc fn を拾わない**＝`findFunc`/`firstFuncByName` の free-fn フィルタは `hasRecv` だけでなく **`isAssoc` も skip**（assoc は `hasRecv=false` ゆえ＝ローカル closure `add` の `add(x:5)` が Core の `Add.add` と衝突して「ラベル不一致」になっていた・assoc は `Type.` 必須で bare 解決しない）。②**conformance は型パラメータ arity も一致で照合**＝`checkConformances` は `t.nameId == cf.traitId` **かつ `t.typeParams.count == cf.traitArgs.count`**（ユーザー `trait Sub: Super`〔0 param〕が Core の `Sub[Rhs]`〔1 param〕に誤マッチして "incomplete conformance" になっていた・arity 違いは別トレイト＝健全な必要条件）。**教訓＝conformance 照合は全 trait def を nameId で走査する global 方式**（モジュール非依存）ゆえ、同名異トレイトの共存は arity/形状で弾く。本当の visibility-aware 照合は未実装（同名同 arity の別トレイトはなお冗長照合）。
- **負リテラル畳み込みは lower で実装済**：`lowerExpr` が `Unary(Neg, IntLit)` を符号付き `Expr.Int`（`isNeg`・magnitude in `value`）へ畳む（`Kind.Minus` パーサ自体は畳まない＝precedence 解決済の lower で畳むので `-128.foo()` は real Neg のまま）。codegen は定数 `-128`／INT64_MIN は `(-9223372036854775807LL - 1)` イディオム・範囲検査/foldConst は `isNeg` を使う。shared @Std/Syntax は不変（マクロ非影響）。新リテラル種を足すなら `Expr.Int` の全 match/構築点に `isNeg` を通すこと。
- **gen モード（`plew gen`・M0）**：`plewc --gen <file>`＝通常コンパイルの変種。`Comp.genMode`/`genMainIdx`、合成は `Codegen/Gen.pw`（`synthGenMain`＝`@[Name]` ごとに `write(s: <Name/>.deriveFromSource(source:"",start:0,end:0))` を arena AST で組む・`isBuiltinDerive` で Eq/Ord 除外）。driver（`_.pw`）で **①argv `--gen` 検出→entry を argv[2] へ ②`@Std/Io` 強制ロード（harness の `write` 用）③ローダ auto-part 抑制 ④checks 後に `synthGenMain`（import hygiene を回避）⑤body emission で user `main` を skip**。ローダ＝パース木の `Directive` ノードで `@[...]` を検出し `<Foo>.gen.pw`（存在時のみ）を同一モジュール part に enqueue。**罠**：`Expr.Str` は span が引用符込み前提（codegen が `start+1`/`len-2`）＝0 長 span は U64 アンダーフロー panic→合成側で実 `""`（2 バイト）を intern して指す。gen 中は user 自身の `main` が生成物を使っても OK（skip されるので未定義参照でも落ちない）＝「同一モジュール derive」が自然成立。auto-part の loud-fail（gen 未実行で `.gen.pw` 欠落）は directive→マクロ分類が要るので将来。

## ビルド・テスト・機能追加手順

- **ビルド**：`./bootstrap.sh`＝C 種 `compiler/plewc.seed.c`→clang→`plewc0`→`compiler/src/_.pw` を自己コンパイル→不動点 cmp（Rust 不要）。`./bootstrap.sh --reseed` で種更新（→ `compiler/plewc.seed.c` を commit）。編集中の高速反復は `./dev-rebuild.sh`（`compiler/plewc` をその場上書き）。**⚠ dev-rebuild は前回ビルドの plewc を使う**ので、miscompile を疑うときは `./bootstrap.sh`〔種から plewc0→現ソース〕で clean 検証する。
- **メタプロ生成**：`./plew-gen.sh <file.pw> …`＝`plewc --gen <file> | clang | run > <file>.gen.pw`。`@[...]` 付きファイルに対し derive マクロを走らせ生成 part を吐く（コミットする・通常ビルドが auto-part）。
- **テスト**：`./test.sh`＝`tests/run/*.pw`（`.out` 照合・任意 `.in`）＋`tests/part/`（複数ファイル）＋`tests/reject/*.pw`（plewc 非ゼロ＝受理の健全性）＋`tests/panic/*.pw`（compile+link 成功・実行は非ゼロ＋`.panic` stderr 部分一致）＋`tests/gen/*/`（`App.pw`＝マクロ＋`@[...]`／hermetic に gen→auto-part→実行→`App.out` 照合・`.gen.pw` は毎回 $TMP で再生成）＋`tests/genreject/`（`plewc --gen` が reject すべき入力）＋不動点。メモリは `ASAN=1 ./test.sh`。**⚠ macOS の ASan は leak 非対応**（`detect_leaks` 不可）＝UAF/二重free は ASan、リークは `MallocStackLogging=1 leaks --atExit -- <bin>` で検証。
- **機能追加＝ADD→reseed→USE**：新機能を plewc.pw の**ソースで使う**には ①`compiler/src/`（または `compiler/std/`）に足す（ADD）→ ②`--reseed` で種更新→ ③ソースで使う（USE）。「ソースが使う機能は常にひとつ前のコンパイラがサポート済み」を守れば不動点は壊れない。**codegen 出力変化・AST フィールド追加・新 preamble 行は reseed 2 回**、**codegen 出力を変えない検査追加は reseed 1 回**。
- **AST フィールド追加のコツ＝デフォルト値**：`val isPub: Bool = false` とデフォルトを付けると既存の構築点が省略でそのまま通る＝多数の構築点を改修せず済む（`<Comp .../>` リテラルも省略可）。
- **⚠ 落とし穴＝コンパイラは `@Std/Io`→`@Std/Core`／`@Std/Syntax`／`Prelude`（全プログラム自動ロード）を import** するので、これらも「コンパイラ自身がコンパイルするソース」。種がまだ受理しない構文/機能を足すと bootstrap が即壊れる＝**機能を先に種へ焼いてから std で使う**（ADD→reseed→USE）。表現スワップ等の大変更は seam（intrinsic 境界）で担保（→ array-struct-plan.md）。
- **arena Expr/AST バリアント追加の罠**：①exhaustive な `match` 全箇所（Lower の `ExprAst`／genExpr・exprType・scanExprInsts 等の arena `Expr`）にケース追加が要る（コンパイル時に non-exhaustive で検出）。共有 `@Std/Syntax` の `ExprAst`/`StmtAst` は **`Codegen/Lower.pw` の `lowerExpr`/`lowerStmt` が唯一の exhaustive match**（パーサは構築のみ・Build.pw は body を slice 出力でノードを match しない）。②`@Std/Syntax` の AST enum にバリアントを足すのは codegen 出力変化＝**reseed 2 回**。

## 運用メモ

- 動作する区切りで**記述的 git タグ**。コミットメッセージは英語＋`Co-Authored-By`。GPG 署名（`--no-gpg-sign` を使わない・たまに pinentry タイムアウト＝再実行で通る／詰まったら手元で `echo test | gpg --clearsign` でパスフレーズをキャッシュ・コミットメッセージにバッククォートを書くと zsh がコマンド置換するので避ける／`-F` でファイル渡し）。
- 不要になった実装は削除し、必要なら git tag から復旧（例：`git checkout stage0-final -- bootstrap`）。
- 言語表面の決定は spec（正典）＋claude/ に即同期し、grep で陳腐化を点検。
- LLVM 化は当面しない＝意味論が揃ってから並行バックエンドとして追加（C は捨てない）。
