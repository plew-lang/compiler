# 作業ログ

> 現在地・**ロードマップ**・運用メモ・**git で拾いにくい再利用知見（罠）**だけを置く。完了した「やった」は git（記述的タグ・コミット）へ。「なぜ・却下案」は [design-decisions.md](design-decisions.md)、パイプライン設計は [architecture.md](architecture.md)、spec からの意図的剥離・暫定（＝残作業の正典地図）は [provisional.md](provisional.md)、メタプロの段取りは [metaprogramming-architecture.md](metaprogramming-architecture.md)。

## 現在地

🎉 **セルフホスト達成・stage0（Rust）退役済み・LLVM 単一 backend（C backend 退役・削除済み）。** 正典コンパイラ＝Plew パッケージ `compiler/src/`（root `_.pw` が `part` で全パートを綴じる 1 モジュール）。自分自身を不動点までコンパイル。構文解析は完全に共有 `@Std/Syntax` の責務（コンパイラとマクロが唯一の共有パーサを通る＝真の 1 AST）。`compiler/src/` 本体は共有パーサで parse→`Codegen/Lower.pw` で arena へ lower するだけ。

- **バイナリ 1 本**：`compiler/plewc`（root `_.pw`＝フロントエンド part 群＋`Backend/Llvm`・libLLVM-C をリンク）。`main`＝`runFrontend()`（共有フロントエンド `Codegen/Driver.pw`・`Comp` を返す）→ `emitLlvm()`。
- **受理健全性は emission 非依存の共有パス `Codegen/Verify.pw`**（`verifyProgram`）＝全 non-generic・non-async 関数を walk し spec 不正を弾く。**型は `Codegen/Infer.pw`（TypeRef ベース型チェッカ）**＝`inferType`（精密型 or 0=不明）＋`typesCompatible`（保守的・確定的不一致のみ reject）。
- **self-host**：`./bootstrap.sh`（IR 種 `compiler/plewc.seed.ll`＋`.seed.runtime.c`→clang＋libLLVM→plewc0→自己コンパイル→IR 不動点）。`--reseed` で種更新（→ 種＋追跡バイナリ `compiler/plewc` を commit）。
- **グリーン**：`./test.sh`（run273/panic13/reject270/part6/fail=0＋不動点）／`./test-gen.sh`（gen10/reject1）。

**実装済みの主要能力**（詳細は spec＋git タグ＋下記 provisional.md／罠。ここは索引のみ）：値意味論＋CoW＋ARC 解放・`unique`/`move`/`borrow`/`deinit`＋move 所有権検査・幅つき整数（overflow/0除算 panic・リテラル文脈型付け）・浮動小数 F32/F64（実用上完成）・**本物の型チェッカ**（型混同 garbage 穴解消）・受理健全性の広範なハードニング・ジェネリクス（単相化・`where` 制約・関連型・aggregate 内 generic-inst フィールド by value）・**ユーザー定義トレイト**（準拠・提供メソッド・bound 越し・継承・トレイト間準拠 `impl B as A`・`any P` 存在型）・**演算子トレイト全配線**（算術/ビット/単項/比較・built-in 算術完全削除）・**拡張 `#Ext`**（struct 完備）・**enum/primitive メソッドディスパッチ**・**async/await 段階 1-3**（stackless SM・イベントループ）・コアライブラリ Plew 化（`@Std/Core`/`Io`/`Process`/`Async`/`Random`・Array＝RawBuffer 床の Plew struct）・ambient `@Std/Core`・**可視性完全強制**・トップレベル変数・メタプロ M0/M1/M2（derive・Dictionary lang item）・**無名レコード**（型/リテラル/`.field`/多値返却/punning/分解/for 分解/construction-field & call-arg context＋method-arg & nested record context）・**newtype**（int underlying 完備＝別型・`as`・算術/比較演算子継承）・**RNG**（`@Std/Random`＋Dictionary ランダムシード）・**整数リテラル基数**（`0x`/`0o`/`0b`＋桁区切り `_`）・**構築フィールドゲート (b)**（cross-module 非 pub フィールド reject）・**lang-item 再定義 reject**・**型パラメータ名衝突→crash 回避 clean reject**・**再帰 generic 値型**（self/mutual・struct/enum・construct/match/access end-to-end）・**型チェッカ soundness ハードニング**（int↔float 暗黙変換/比較 reject・`any P` 非準拠 reject・generic bare-`T` 引数一貫性・closure body 検査＋closure-arg 戻り型・call/method VALUE 引数 type-check・if/while 条件 Bool 必須・**primitive binop の RHS 型一致**〔`1I64+true`〕・**`&&`/`||` Bool オペランド**・**比較の cross-type**〔`"a"==1I64`〕・**複合代入の型一致**〔`x+=true`〕・**`as` の Bool↔numeric 拒否**・**配列 append〔ネスト含む〕/ dict literal+insert の要素・K/V 型一致**・**range 両端の型一致**・**field/default arg 値の型**・**比較/論理結果の Bool 型付け**・**void 結果を値に使うのを拒否**・**単項 String 拒否**・**async 本体検査**・**try error 型一致**・**call-site `inout` キーワード＋mutable place**・**assoc fn 引数の型**・**準拠 witness の返り値型**・**generic-receiver method の concrete primitive 引数**・**非リテラル配列値の要素型**・**dict get/getOr/contains 等の K/V**・**Ref/WeakRef の value 型**・**関数値の署名一致〔binding/return/arg/field/element 全位置・`tcCheckValue` 統合〕＋呼び出し引数型**・**generic 型注釈の arity**・**match の未知 variant**・**float range は非反復**・**closure の missing-return**・**準拠 witness の関連型解決返り値**・**`any P` メソッド結果型**）。reject 184→267。**✅ Section A（soundness・受理健全性）完了**（generic 本体 abstract 検査・use-after-move・generic-receiver grounding・factory 公開ゲート・他多数＝下記ロードマップ A 参照。残るは hidden-meaning/cost の機能追加）。

## 🎯 ゴール＝「3 大機能を除く spec 完全準拠」

> **2026-06-12 マイルストン：Section A（soundness・受理健全性）完了。** spec-invalid を accept する穴は全て塞いだ（generic 本体 abstract 検査・provided-method arity・default 自己完結・deinit on unique・重複 type-param/trait-req・bare return・closure ラベル/fn 型ラベル・inout place・array↔scalar・use-after-move・generic-receiver grounding・factory 公開ゲート・unicode escape・float→int as・…）。残るは Section B（hidden meaning＝spec-valid を reject／silent 逸脱）と C（hidden cost＝leak）で、**いずれも parser/backend codegen に跨る中〜大の機能追加**（From/TryFrom・literal match パターン〔`match x { 0 => … }`〕・名前空間 import〔`Io.print`〕・newtype 非 int 継承・Iterator sum/enumerate/zip・assoc val・global 前方参照 等）＝各々 focused な実装が要る。残スコープは下記 B/C と provisional.md。

**並行 spawn・循環回収・パッケージ管理（M3）の 3 つを意図的に後回しにし、それ以外で Plew が完全に仕様通り動く状態を目指す。** 残作業の正典地図は [provisional.md](provisional.md)（＝現コンパイラが spec から意図的に剥離している箇所のカタログ・`✅` でない項が残作業）＝**この 3 つ以外の剥離をすべて潰す**のがゴール。完了の目安＝provisional.md が（3 大機能・spec-future を除いて）すべて `✅` になり、spec 由来の reject/run テストが網羅される。

**スコープ外（このゴールに含めない）**：
- **3 大機能**＝spawn（spec/14）・循環回収（design-decisions）・パッケージ管理（M3）。
- **spec 自身が additive/将来送りにしている項**＝固定長配列 `[E; N]`・const generics・`Slice`・部分文字列・`USize`/`ISize`。

**ツール要件（達成済）**＝任意パスのバイナリ単体で `plew build`/`plew gen` が動く（ⓐ＝relocatable driver＋`compiler/plewc`＋clang・symlink 解決）。真の単一バイナリ ⓑ（plewc が clang を内部 spawn・LLVM 直接オブジェクト出力）は additive な将来 upgrade（要ユーザー確認）。

## 🗺️ ロードマップ（残作業のみ・soundness 優先）

進め方＝[provisional.md](provisional.md)（残作業の正典カタログ・`✅` でない項が残作業）を走査し各剥離を green 増分で潰す→区切りごとに commit＋push＋本ログ更新。優先＝**soundness（spec-invalid を accept しない）** ＞ **hidden meaning（spec-valid を reject／silent 逸脱）** ＞ **hidden cost（leak）**。完了済みは上の capability 索引＋ provisional の `✅`。**以下は残のみ。**

### A. soundness（spec-invalid を accept する穴）＝🎉 完了

**2026-06-12 に Section A を完了**（generic 本体 abstract 検査・provided-method arity gate・use-after-move・generic-receiver 引数 grounding・factory 公開ゲート (a)・closure/fn 型ラベル・inout place・array↔scalar・default 自己完結・deinit on unique・重複 trait-req/type-param・bare return・break/continue・unicode escape・float→int as ほか・~130 probe で contained 穴は尽きた）。詳細は provisional.md の各 `✅` と git。**設計判断で残したいもの**＝generic 本体検査は **Rust 流（宣言時 bound-aware）＋entry-module gate**〔std は trusted・user module だけ厳格〕で確定（「template を型パラメータ-aware で全 verify」案は false-reject と soundness 緩めをトレードするだけで✗と実証）＝[generic-abstract-checker.md](generic-abstract-checker.md)。generic-receiver grounding は **横断 refactor 不要で解決**（`recvOwnParamCount` で method-own param を abstract 扱い）。

残る soundness は spec が将来送りの 1 件のみ：**重なる inout ②③**（部分/添字重なり＝`a.merge(inout a.field)`・`arr[i],arr[j]` の distinct 証明）＝spec 通り lint＋限定ランタイム panic（ケース①構文同一は実装済）。

### B. hidden meaning（spec-valid を reject／silent 逸脱）＝**実質的に完了**

> いずれも parser や backend codegen に跨る中〜大の機能追加。各々 ADD→reseed→USE で 1 機能ずつ focused に。価値/規模の目安を付す。
>
> **2026-06-13：Section B の hidden-meaning 穴は概ね閉じた**（ユーザー確認済）。本日完了＝**float→int TryFrom**〔切り捨て〕・**type/value import alias**〔`with { Real as Alias }`〕・**dir module 解決**〔`/Models`→`_.pw`〕・**generic assoc-fn emission**〔`Box[T].make`〕・**`pow`/`Pow[Exp]`**・**String UTF-8 検証**〔`<String.convert from=bytes/>`＋unchecked rename〕・**cross-sign TryFrom**〔I64↔U64〕。**✅ 一般 Chain トレイト（ネスト含む完成）**〔2026-06-13＝ユーザーが「`?.` を理由なく特別視しない」原則で skip 判断を覆し実装＝Optional 専用でなく `trait Chain` 脱糖で任意の value-or-empty 型が `lo?.n`・ネスト `a?.x?.y` も可・Mono `rewriteOptChainToChainMatch` で `match base.chain(){...}` へ in-place rewrite・no-arg factory を期待型から ground する一般能力を付随追加・ネストは各段結果型を `Comp.chainResults` 側マップに記録し typeOf/exprTypeRef が復元＋各 chain match は自段記録型で arm を ground〔核バグ＝内段が外段期待型を継承し factory を誤 instance 化していた〕・test run chain_trait〔Some/None＋ネスト〕・詳細は provisional.md〕。**残は additive/見送り**＝**sum/zip**〔ユーザー判断で見送り＝additive・numeric tower〔Zero atom〕設計は未決のまま保留・なお associated-type bound `Iterator[Item=T]`＋`T.zero()` は動くと確認済〕・**narrower-source/float→unsigned TryFrom**〔boilerplate〕・**import パス正当性厳密検査**〔reject-hardening・hidden-meaning でない〕。**次の主戦場は C（hidden cost＝leak／循環回収）**〔3 大機能の 1 つ＝要ユーザー確認〕。
>
> **2026-06-13（続）：トレイト間準拠 `impl B as A` をベア許可＋頭なし blanket を reject 化**（ユーザー判断＝spec の「ベア `impl B as A` 不可」は根拠なし＋Iterator→Iterable 設計と矛盾ゆえ撤回）。実装＝`instantiateTraitSubjectConforms`〔`isTraitName(subject)` で `impl B as A` を検出し、B 準拠の各具体型へ `applyBlanketTo` でメソッド複製＋conform 記録＝A が対象型のベア表面に自動随伴〕・拡張内 `impl B as A` は `Conform.extId != 0` で除外し view-gated 維持〔`extension_trait_subject` の `p#AsShow.show()` を壊さない〕。**頭なし `impl[T] T as A where T: B` は reject**〔`Verify.checkBlanketHeadless`＝`isBlanketConform` 検出・旧 `instantiateBlanketImpls`〔頭なし実装〕は退役・`typeSatisfiesBlanketBounds`/`blanketHasBound` 削除〕。spec/08,09＋design-decisions 同期済。test run trait_blanket〔`impl Named as Show`〕・reject blanket_headless。
>
> **2026-06-13（続々）：トレイト間準拠の衝突 `#` 経路選択（直接呼び）実装**（ユーザー選択）。S が P→R・Q→R の 2 経路で R に届くとき＝conferral は各経路の witness を `srcTraitId=主語トレイト` で tag して併存〔`applyBlanketTo` に `guardHi`〔originals 限定 dup 防御で sibling が互いを隠さない〕＋`recordConform`〔multi-path は bare conform 記録せず〕追加〕。bare `s.r()` は findMethod 既存の曖昧検出で reject・`s#P.r()` は `findExtMethodById`〔`srcTraitId==#P`〕で経路選択・single-path は bare 表面に載る〔直接呼び＋`where _: A` 境界〕。test run trait_subject_select・reject trait_subject_ambiguous。run=300 reject=279。**別件で気づいた pre-existing バグ**：ユーザー struct を `T` と命名すると慣用の型パラメータ名 `T` と衝突しジェネリック本体検査が誤 reject〔今回の機能と無関係・未修正〕。
>
> **2026-06-13（続×4）：view-aware monomorphization 完成＝`#` ビューをジェネリック境界越しに渡せる**（ユーザー判断＝trait 形 `#P` を型レベルビューへ格上げ・spec/09:192 改定済）。`f(a: s#P)` where T: R も `g(x#Ext)` where T: A〔spec/09:269・従来 broken〕も成立。実装＝(1) `#`-view 引数の view を `argViewRef`〔Mono inferFnArgs〕／`exprTypeRef`〔既存・backend〕が `TypeRef.viewExtId` に載せ推論で `T→S#P`。(2) `checkFnBounds` を `viewConformsTo`〔S が主語トレイト/拡張 subject に準拠し `impl subj as A` 存在で充足〕で view-aware 化。(3) `bindOneParam` が grounded param の viewExtId を local に伝え body `x.m()` を viewChain dispatch。(4) `typeRefEq` に viewExtId を加え `useR[S#P]`/`useR[S#Q]` を別 instance 化〔generic 名は gf<N> index で symbol 衝突なし〕。(5) multi-path witness を `Func.viewOnly` で bare 解決から除外〔extId ゲートと同形・bare `s.r()` は `viewOnlyMethodExists` で loud 曖昧 reject〕。(6) view 不一致ガードは `tyNameConcrete` で concrete param のみ〔generic param は view を吸収〕。spec/08,09＋design-decisions 同期済。これで**トレイト間準拠 `impl B as A` は spec/09 完全実装**（ベア所有者・拡張第三者・多経路 `#` 選択・境界越し・頭なし blanket reject）。run=300 reject=279 gen=11 fixpoint OK。
>
> **2026-06-13（続×5）：トレイト間準拠の連鎖（推移）＝実装**（ユーザー判断＝連鎖入れる・`#` は impl 持ちトレイトのみ・衝突は消えるまで任意段で絞る）。`impl C as B`＋`impl B as A` で C 準拠型→A へ推移。`instantiateTraitSubjectConforms` を**fixpoint 反復**化〔live conform 走査・新規 conform が尽きるまで反復・停止性は (型,トレイト) 有限増加〕＋`applyBlanketTo` 冪等化〔`conferredCopyExists`〔name+recv+srcTraitId〕で再 copy 防止・sibling 多経路は srcTraitId 違いで併存〕。直接呼びと `where _: A` 境界が一致〔「`s.a()` は動くが境界は落ちる」非対称を解消〕。test run trait_subject_chain。spec/09 に推移準拠＋「`#` は経路上の impl 持ち主語のみ・常に一意化可」を追記。run=301 reject=279 gen=11 fixpoint OK。
>
> **2026-06-13（続×6）：連鎖×衝突（中間ホップが多経路）＝実装＝トレイト間準拠 spec/09 完全実装に到達**（ユーザー裁定＝「`s#C.a()` を実装するほか一意化不能」）。`impl C as B`＋`impl D as B`＋`impl B as A` で、`a`（B 経由）の本体が `self.b()` を呼ぶが `b` は C/D の 2 経路ゆえ bare `s.a()` は原理的に曖昧→`s#C.a()`/`s#D.a()`（分岐トレイトを絞る）で初めて使える。**viewRoot モデル**：(1) `Conform.viewRootId`＝多経路 conferral は分岐主語トレイトごとに別 conform を記録〔0=bare〕。(2) `applyBlanketTo` に `viewRoot` 引数＝`cand.viewRootId` を上流へ継承、初出多経路は `subjectPathCount>1` 検出で `vr=cf.typeId`。(3) `Func.selfViewId`＝witness 本体は root view で `self` 束縛〔backend `genLlvmFuncBody` が `recvRef` に `viewExtId=selfViewId`〕ので内側 `self.b()` が同経路解決。(4) 多経路 witness は `viewOnly=true`〔bare 除外〕＋`instantiateProvidedMethods` も `viewRootId!=0` skip。(5) `conformWithRootExists`/`conferredCopyExists`〔srcTraitId キー〕で fixpoint 再 copy 防止。直接 `s#C.a()`＝`findExtMethodById`・境界越し `useA(x: s#C)`＝view-aware mono・bare は `viewOnlyMethodExists` で loud reject。test run trait_subject_chain_conflict〔16/512・直接＋境界〕・reject trait_subject_chain_conflict_bare。**残（additive のみ）**＝rule 2 強制〔`#X` は impl 持ちトレイトのみ・診断未〕・トレイト引数 aware の多重 conformance 区別。run=303 panic=14 reject=281 gen=11 fixpoint OK。
>
> **2026-06-13（続×7）：単一経路（曖昧でない）の `#P` ビュー選択を valid 化**（ユーザー指摘＝「普通に実装したら valid になるはず・あえて弾いてるか実装が tricky かの二択」→後者だった）。`impl S as P`＋`impl P as R` のみのとき `s#P.p()`〔P 自身の要求を S が直接 witness〕/`s#P.r()`〔単一経路 conferral〕が `genLlvmExtMethod` で「no such extension method」誤 reject〔意図的でなく view 解決 `findExtMethodById` が tag〔extId/srcTraitId〕しか見ず直接準拠 witness〔srcTraitId==0〕を見落とす穴〕。spec/09:209「`#` はトレイト相対で直接準拠でも解決」を正典とし、192「必要なのは曖昧時のみ」と整合〔冗長だが valid〕。修正＝view-tag 空振り時の**冗長ビュー fallback**＝`traitProvidesReqName(P, foo)`〔Decl 新ヘルパ・P が foo を要求〕かつ `conformExists(S, P)`〔ビュー実在〕の両ゲートで bare `findMethod` 解決（provenance 保持・誤源 `s#Q.p()`/`s#Z.p()` は依然 loud reject）。test run trait_view_single_path・reject trait_view_wrong_source。run=303 reject=281 gen=11 fixpoint OK。これで `#`（メソッド源／準拠経路セレクタ）は単一経路の冗長使用も含め spec/07,09 通りに動く。

**✅ 2026-06-12 に完了した B 項（match クラスタ＋assoc val＋global 順序＋import クラスタ＋Output!=Self＝1 セッション 12 機能）**：
- **match クラスタ**＝~~literal pattern~~・~~式位置 `panic`/`return`（value-match diverge arm）~~・~~capture-binding `val x =>`〔scalar／enum-dispatch の statement・value 両位置とも〕~~＝✅。**arm ガードは spec/11 が意図的に持たない**＝一度実装したが revert し parser で loud reject（絞り込みは束縛＋ネスト match か前段 `if`/`guard`）。残＝**ネストパターン**・**`guard` 文/条件チェーン**〔下記・本来 spec にある絞り込み機構〕・到達不能アーム警告。
- ~~`assoc val`（static 定数 `Type.NAME`）~~・~~**generic assoc-fn emission**〔`Box[T].make`〕~~＝✅。**generic assoc fn**＝self を持たないので generic free-fn 機構（FnInst/declareGenFns/emitGenFns）にそのまま相乗り：mono の `registerAssocInst`〔`Type.make(args)` の type-name receiver を assoc 解決し isGenericFreeFn なら call 引数から型引数推論して FnInst 登録〕＋backend assoc 分岐〔isGenericFreeFn なら `llvmInferFnArgs`+`genFnSlotFor`+env 下で emit・`genLlvmFuncBody` は `hasRecv=false` で self なしに params を index 0 から束縛済〕。単一/多 type-param〔`Pair.of(a:A,b:B)`〕・複数インスタンス・nested assoc〔`Box.dup`→`Pair.of`＝transitive collection〕が end-to-end・test run generic_assoc_fn。残〔限定〕＝戻り型だけに type param が現れ args から推論不能な assoc〔`Box.empty()->Box[T]`〕は expected-type 推論未実装で従来 error のまま。
- ~~global 前方参照~~＝✅（依存順トポロジカルソート・循環 reject）。
- ~~名前空間 import `Io.print`~~・~~path alias `import … as F`~~・~~name alias `with { real as alias }`~~・~~**type/value の import alias**~~＝✅（import クラスタ完成）。**type/value alias** ＝`with { Real as Alias }` で型/enum/値を別名 import＝中央ヘルパ `aliasReal`〔モジュール局所・no-op fast path で既存挙動不変〕を型名解決の funnel に挿入：`structIndexByName`/`isTypeName`/`typeInfoOfName`〔carried name を canonical 化〕/`fieldType`/`knownNamedTypeId`〔型同一性の根＝alias と real が同一 id〕/`variantExists`/`variantIndex`〔enum tag〕/`enumIndexByName`〔front〕/`enumIdxByName`〔backend〕/`assocRecvName`〔`Alias.fn()` 脱糖〕。struct alias〔注釈・default factory 構築・field access〕・assoc-fn via alias〔`Account.make()`〕・value alias〔`greet as hello`〕・enum alias〔構築・match arm・tag〕が end-to-end・test part importalias。~~**`_.pw` ディレクトリ解決**~~＝✅（`import /Models` が `src/Models.pw` 無しのとき `src/Models/_.pw` にフォールバック＝Loader `finishModulePath`〔`<base>.pw` 優先・無ければ `<base>/_.pw`・bare `/` ルートも `src/_.pw`〕・`/`・`./`・`../` 全ルート・std は `Foo.pw` 併置のまま不変・test part dirmodule）。残＝パス正当性厳密検査。
- ~~**`Output != Self`**（operator trait の `type Output = X` を結果型に）~~＝✅（`AssocBinding.traitId` で trait 別解決・`operatorOutputType`）。

**✅ String UTF-8 妥当性チェック（spec/02 の「String は UTF-8 妥当」不変条件の穴を閉じる）**：安全な公開コンストラクタ `<String.convert from=bytes/>`＝`impl String as TryFrom[Array[U8]]`→`Result[String, Utf8Error]`〔Unicode well-formed-UTF-8 表＝overlong/サロゲート/範囲外を弾く・`utf8FirstInvalid` が最初の不正バイト offset を返す・`Utf8Error.index`〕。unchecked 床は `stringFromBytesUnchecked` へ**リネーム**〔旧 `stringFromBytes`・~20 箇所＝std パーサ＋コンパイラ本体・**ADD→reseed→USE**＝backend intercept に新名追加→reseed→全 call site rename→reseed〕＝export 維持で既知妥当バイトの hot path は無コスト、名前で escape-hatch を明示〔Plew に crate-internal 可視性が無く std/コンパイラが cross-module で使うため un-export 不可・ユーザー判断で rename を選択〕。**conformance witnessing の array trait-arg 対応**＝`paramSelectorEqSubst` に `substReqParamArrayElemRef`〔req param が `Array[Elem]` に置換されるとき witness の array element と比較・以前は head "Array" vs witness elem "U8" で誤 reject〕。test run string_from_bytes。

**✅ primitive/enum メソッドディスパッチ・クラスタ（memory が「最大ブロッカー」と記す領域・2026-06-12）**：backend の `bindOneParam` が generic param `x: T` の grounded 型を tracking する範囲を int 限定から拡張＝**Bool**〔`Dictionary[Bool,V]`・`impl Bool as Hash`・test run dict_bool_key〕・**enum**〔`fn f[T](x:T) where T: Describe` を enum 値で呼ぶ trait dispatch・test run generic_enum_dispatch〕。さらに **inline construction 引数の T 推論**〔`f(x: <Color.Blue/>)`／`<Point x=1/>`＝`llvmArgTypeRef` に Make ケース追加・以前は exprTypeRef に Make 無く 0 推論で instance miss〕。**残＝ユーザー struct を dict キー**〔`@[Hash]` derive の 2-phase gen ワークフローと conformance の chicken-and-egg・別系統〕。

**残りの B 項（大物＝専用 focused session 推奨）**：
- **factory 機構の残**＝**custom `factory [name](params) { body }`＋fallible `optional factory`/`result[E] factory`**（→ Optional[Self]/Result[Self,E]）。〔bare `factory` publish＋公開ゲート (a)〔`structHasPubFactory`〕は✅済〕。**これが `From`/`TryFrom` の前提**。
  - **実装スケッチ（assoc-val 脱糖と同型・低リスク経路）**：① **named factory `factory name(params) { body }`** を **assoc fn へ脱糖**（spec/05＝`<Type.name attrs/>` で呼ぶ・名前付きは camelCase）。parser で `factory` の後が Ident なら named・`(` なら anonymous。② **Self 戻り**＝`MemberAst.isFactory: Bool=false` フラグ追加（default 付きゆえ既存構築は無変更）→ `lowerMethodMember` が `isFactory` なら assoc fn の ret を impl 型（`recvN`）に。③ **`<Type.name attrs/>` の routing**＝Make ノード〔`isEnum=true variant=name`〕で `typeHasAssocFn(Type, name)`〔assoc-val で実装済〕が真なら **post-load pass で `Method(Ident(Type), name, args)` へ書き換え**（MakeField→Arg 変換・enum variant は assoc fn でないので不変・既存 assoc-fn 機構を再利用）。④ **anonymous `factory(params)`** → `<Type attrs/>`〔memberwise と label 集合でオーバーロード〕は別途。⑤ **fallible**＝`optional`/`result[E]` 前置（文脈依存 kw・factory 修飾位置のみ）→ ret を `Optional[Self]`/`Result[Self,E]` に。⑥ **From** は無名 factory〔label `from`〕で `<T from=x/>`＝`x as T` 糖衣・**TryFrom** は named `convert`〔`<T.convert from=…/>`〕。①②③が最小の動く slice。
- **`From`/`TryFrom`**＝ユーザー型は✅完了：`factory(from:S)`＋`x as T`/`<T from=x/>`（From）・`result[E] factory convert(from:)`＋`<T.convert from=…/>`（TryFrom）・**`try` の異エラー型 From 変換**〔E≠E' のとき E の `from` factory で E'→E 変換・`typeRefHasFromFactory`/`typeRefFromFactoryIdx`/`resultErrArgRef`・genLlvmTry Err 枝で from factory 呼び・Verify は from factory ある時のみ E≠E' 許可〕＝✅。〔✅ mixed float context・✅ float→int `as` の clean reject〕。✅ **float→int 変換 `TryFrom[F64]`**〔`<IX.convert from=f/>`→`Result[IX,RangeError]`・I8/I16/I32/I64・**0 方向切り捨て**〔C/Rust 慣習〕・NaN/±inf/範囲外で Err〔`isNanF64`＝`fcmp uno` を順序比較より先に・`f64ToI64`＝`LLVMBuildFPToSI`・narrow target は I64 切り捨て後 range-check〕・**float 単項 Neg バグ修正**〔`LLVMBuildNeg`→`LLVMBuildFNeg`〕・test run float_to_int／reject cast_float_to_int〕。✅ **factory-as-trait-requirement（キーストーン）**＝trait が `factory` 要求を持てる〔`trait TryFrom[Source] { type Error  result[Error] factory convert(from:) }`〕。共有 @Std/Syntax の trait parser が `[optional|result[E]] factory name(params)` 要求を selector で `tr.reqs` に登録・既存 witnessing〔trait-impl factory が `cf.witnessed` へ・`reqReturnMismatch` は Result/Optional/Self 形に寛容〕＋builtin-impl gate〔`methodWitnessesAConformance`〕で `impl I8 as TryFrom[I64]` の convert factory を受理。**③ 一般 Chain トレイトもこれで unblock**。
✅ **プリミティブ整数の縮小 TryFrom**（spec/12）＝`<IX.convert from=wide/>`→`Result[IX, RangeError]`〔I8/I16/I32 from I64〕。`as` は全域専用ゆえ縮小は `convert` factory で range-check〔built-in `<`/`>`〕＋truncate〔新 `truncI8/I16/I32` intrinsic・`coerceInt` trunc〕。`RangeError`〔Core・export・offending 値を保持〕。test run tryfrom_narrowing。
✅ **文字列→I64 パース TryFrom**（spec/12）＝`<I64.convert from="123"/>`→`Result[I64, ParseError]`〔`impl I64 as TryFrom[String]`〕。**負空間で累算**して I64.min〔絶対値が max 超〕も表現・overflow は checked `*`/`-` が panic する手前で reject・空/単独符号/非数字は ParseError。`ParseError`〔Core export・入力保持〕。新コンパイラ機能なし（キーストーンのみ）＝単一 reseed。test run tryfrom_parse〔min/max 境界・overflow・非数字・空・単独符号〕。
✅ **unsigned 整数の縮小 TryFrom**（spec/12）＝`<U8.convert from=wide/>`〔U8/U16/U32 from U64・upper-bound のみ・`truncU16`/`truncU32` intrinsic〕。**＋pre-existing signedness bug 修正**＝generic Optional/Result 越しの unsigned 要素〔`Optional[U8].unwrapOr->U8`〕が signed print〔200→-56〕されていた＝`methodReturnKind` が STRUCT receiver の env しか張らず ENUM receiver で `T` が abstract のまま signed 既定→`setEnumEnv` fallback で `T->U8` ground〔kind 3・zext〕。test run tryfrom_unsigned/optional_unsigned_elem。**整数 narrowing matrix 完成**〔signed I8/I16/I32 from I64・unsigned U8/U16/U32 from U64〕。**残（trivial additive）**＝from I32/I16・cross 符号・他幅の文字列パース＝機構完成済 copy 増分。
- ✅ **newtype 非 int underlying のメソッド/フィールド継承**（`UserId = String`）＝達成。3 点修正：(1) **`kindFromTySpan` が newtype→underlying 解決**〔newtype local の valueKind＝underlying と一致・以降 `primLangItemRecvName` の `valueKind==1→String` 経路でメソッドが自動 dispatch・`isStringBytesField` で field も解決＝当初恐れた多点配線が 1 点に集約〕・(2) **Cast の非 int newtype re-tag no-op**〔target が newtype〔wrap〕か operand の type が newtype〔unwrap・tref 追跡で判定〕なら ov そのまま返す・Verify が legality を先にゲート〕・(3) **`valueKind` Method 枝に `primLangItemRecvName` 経由の戻り kind 解決**〔typeOf は newtype 継承メソッドを見えず Bool 戻りが int 既定→`-1` 誤表示するのを防ぐ〕。test run newtype_string_inherit・reject newtype_no_implicit_unwrap。✅ **`export newtype`**〔共有 parser `parseExportAst` が contextual keyword `newtype` を認識せず ReExport に誤落ち→Ident arm に newtype 枝・`lowerExportItem` に NewType recordExport・test part exportnewtype〕。✅ **user 定義 `impl` を newtype に書くのは reject**〔spec/10：newtype は underlying の実装を全自動継承・ユーザーは impl を書けない〔振る舞い追加は struct ラップ〕・`checkImplOnNewtype`・以前は silent accept で dispatch 壊れ・test reject impl_on_newtype〕。✅ **trait 準拠の自動継承**〔`typeConformsTo`/`conformExists` が newtype→underlying 委譲・`print(u)`/`print(m)`/user trait が `where T: Trait` を直書きで満たす・dispatch も prim 経路・soundness 保つ・test run newtype_conformance_inherit・reject newtype_conformance_missing〕。✅ **struct underlying のメンバ/準拠継承**〔`localStructIdxFor` が newtype→underlying struct idx を返す＝struct dispatch 経路に乗る・`w.v`/`w.mark()`/`needs(x: w)`/`w as Plain` が end-to-end・test run newtype_struct_inherit〕。✅ **既定 factory 継承（直接構築 `<Brand code=… />`）**〔3 点 newtype→underlying：`makeFieldType`〔Lower〕/`fieldType`〔Resolve〕/`genLlvmMake`〔backend〕・test run newtype_factory_inherit〕。✅ **unique/deinit 自動継承**〔`typeIsUnique` が newtype→underlying 解決＝copy-reject/move-mode 要求・deinit 走行は `localStructIdxFor` 経由で自動・test run newtype_unique_inherit・reject newtype_unique_no_copy〕。✅ **named/fallible factory 継承**〔`typeHasAssocFn`/`findAssoc`/`isTypeName` newtype 解決＋`typeOf` assoc 戻り Self→newtype 置換・test run newtype_factory_inherit・reject newtype_factory_self_distinct〕。**newtype は spec/10 完全準拠**（メソッド/フィールド/演算子/trait 準拠/factory〔default/named/fallible〕/unique/deinit/export/`as`・user impl reject）。
- **Iterator 残**＝`sum`（Zero/数値タワー待ち＝大）・`zip`（generic 抽象 `T::Item` 未対応＝別系統）。✅ **`enumerate` 完成**（spec/02,08）＝**generic record 基盤を実装して達成**：
  - ✅ **(1) 型チェッカ grounding**＝`groundTypeRef` args==0 枝に **`groundRecordStruct`**〔record struct〔`(` 始まり〕検出・各 field ground・canonical 名再構築して登録済 concrete record ref を返す・`recordTypeKeyFromRef`〕。
  - ✅ **(2) record-as-enum-payload 構築**＝`buildEnumValue` が record payload field に `st.expectedTy`=grounded record を set・`genLlvmMake` が empty 名 record を `st.expectedTy` から解決（test run record_in_optional）。
  - ✅ **(3a) chained field access on method-result record**〔`o.unwrapOr(...).index`〕＝`exprStructIdx` の Method 枝に enum-receiver 分岐〔recv instance ref ＋ method 戻り型を env 下で ground〕。
  - ✅ **(3b) `EnumerateIter[I,E]` adapter ＋ `enumerate()` ＋ adapter mapping**＝`n` field default `=0U64`〔inline adapter build 用〕・**binding 形 `for p in it.enumerate()` ＋ label 分解 `for (index: val i, value: val v) in …` 両方動く**（test run iter_enumerate）。
  - **残（小）**＝record literal を method arg に渡す grounding〔`r.unwrapOr(fallback: (x:1,y:2))`＝call site で receiver instance env が要る generic-method-arg gap〕。
- ~~**条件チェーン＋`guard` 文**~~＝✅**ほぼ完了**（spec/11・match ガード revert の正規代替）。**if-let**〔`if PATTERN = expr [&& boolClause…] { then } [else]`〕・**while-let**〔`while PATTERN = expr { body }`〕・**`guard` 文**〔`guard cond/PAT=e { else } rest`＝rest を成功枝に包んで binding-escape〕＝全部済（**パーサで `match`/`if`/`while` へ脱糖・arena/backend 変更ゼロ**・`detectRefutableBinding` backtrack・scrut は `parseBinAst(minPrec:3)` で top-level `&&` 手前まで・trailing `&&` Bool 節は parens で `||` 内包〔spec 通り〕）。test run if_let/while_let/guard_stmt。~~Bool-first-then-binding〔`c && P=e`〕~~＝✅（`condChainHasBinding` で `&&` 全クローズを走査・binding がどのクローズにあっても cond-chain 脱糖へ・pure-Bool は plain-expr 経路維持で `||` 対応・if/if-expr/while/guard 4 箇所・test run cond_chain_bool_first）。~~guard else 発散 enforce~~＝✅（`blockDivergesAst`/`stmtDivergesAst` で構造的に最後の文が return/panic/break/continue か all-diverge な if/match か検査・非発散は loud reject・test reject guard_else_no_diverge）。**条件チェーン/guard クラスタ完了**。
- **一般 Chain トレイト `?.`**（現 Optional 具体・実装者は Optional のみ＝spec も Result 除外ゆえ実用価値低・**factory-as-trait-requirement キーストーン済ゆえ unblock**＝`trait Chain { type Value  fn chain()  factory fromValue/empty }` が書けるように・現 backend `genLlvmOptChain` は Optional の表現に直書きゆえ一般化は chain()/fromValue/empty 呼び＋結果型 O の決定規則の整理を要す）。✅ **`Pow`/`pow`**（spec/12＝`**` なし・bare `pow(base:, exp:)` が base 型の `Pow[Exp]` へ dispatch）＝**達成**：`trait Pow[Exp]`＋整数 `Pow[U64]` conformance〔checked-mul ループ・overflow panic〕・backend `genPowCall`〔`genLlvmCall` で name=="pow" 横取り→`findAssoc(llvmArgTypeRef(base),"pow")`→assoc emit・`exprTypeRef` の Call 枝で結果型を base 型に復元〕・user 型 Pow も bare dispatch・`Type.pow(base:,exp:)` の型修飾形は既存 assoc 機構で自動・float pow〔`F64 as Pow[F64]`・超越 intrinsic〕は additive 未提供・test run pow／panic pow_overflow。
  - ~~Bool dict キー~~＝✅（`Dictionary[Bool, V]`＝backend の Bool primitive method dispatch を配線＝`bindOneParam` が Bool-grounded な generic param `key: K` の型を tracking〔以前は int-grounded のみ〕＋`primLangItemRecvName` が Bool receiver を grounded env から解決・`impl Bool as Hash`〔1/0・cast 無し〕・test run dict_bool_key）。
  - ~~ユーザー struct を dict キーに~~＝✅**完成**（`Dictionary[Pair,I64]` with `@[Hash]`/`@[Eq]` derive・2-phase gen を end-to-end・test gen structdictkey）。2 つの backend fix：(a) **gen-discovery**＝gen mode で `scanType` が「type arg が derive 対象を名指す generic inst」を登録しない〔`refArgNamesADeriveTarget`・`Mono.pw`〕＝phase 1 の chicken-and-egg〔`Dictionary[Pair]` の `where K: Hash` を未生成 `Pair.hash` に対し単相化〕解消。(b) **struct `==` vs String**＝2-field struct〔`Pair{i64,i64}`〕は String の `{ptr,i64}` と同 LLVM layout ゆえ valueKind が grounded generic-`K` struct key を String と誤読→monomorphized `Dictionary[Pair].find` の `self.keys[idx] == key` が `plew_str_eq` を呼び {ptr,len} 誤解釈で SIGSEGV〔`Backend/Llvm.pw`〕。**lhs が登録 struct なら byte-compare path に行かない**ガードで Eq witness 経路へ。lldb で `plew_str_eq(a=0x1,na=2)`〔Pair{1,2} の field を String と誤読〕を特定。
  - ~~match ネストパターン~~＝**B 項ではない**（spec/11 line 70/100 が意図的に単層パターン＝フィールドは `val name`/`_` のみ・絞り込みは「束縛してからネスト match」）。現状の reject は spec 通り＝パーサで clean error 化済（`field: Point(...)` に「patterns are single-level — bind then match」・test reject nested_pattern）。

> **次の推奨着手順**＝① **factory 機構の残**（custom/fallible factory）→ ② **From/TryFrom**（①が前提）が最大価値。並行で **Iterator backend**（enumerate/zip/sum）か **newtype 非 int**（risk 高）。小物（条件チェーンの残・Chain/Pow/nested）は個別に。
- **トレイト/拡張小物**＝トレイト引数 aware の多重 conformance 区別・コンテナ不変性 `Array[P]`≠`Array[P#Ext]`・型レベル chained `Type#A#B`。

### コード整理 TODO（純リファクタ・観測挙動不変）

- **冗長な `if a == b { } else { BODY }` → `if a != b { BODY }`**：self-host 初期の書き癖で empty-then-else 形が compiler/src・std に散在（`!=` は使えるのに）。一括修正（同種の `if a { } else { BODY }` も対象）。要 dev-rebuild＋test＋不動点。

### C. hidden cost（leak・観測挙動は正しい・最後）

- **backend 全体の ARC drop**＝array/String/any box・closure 一時・Ref pointee 深い release・mono struct/enum の share/release を scope-exit で。観測挙動不変ゆえテストしにくい＝`leaks`/ASan で検証。

### 横断＝根治 refactor（複数項の根・任意）

- **型パラメータ判定のスコープ化**＝`isTypeParamName` のグローバル名照合が根。これを直すと (i) 単一大文字 struct 名が std メソッド型パラメータ（`fold[B]` の `B`/`F`/…）と衝突して generic 引数に使えない問題、(ii) 型パラメータ名衝突〔現状 crash→reject で回避〕が解消。name-based でなく**スコープ付き param 同一性**へ。〔generic-receiver grounding は本 refactor 無しで解決済ゆえ優先度は下がった〕

### スコープ外（3 大機能・spec-future）

- **`spawn`/`JoinHandle`/チャネル**（async 安定後・実スレッド pthread・境界で CoW eager 実体化・`Ref` 不可・`local` 伝染解析・言語仕様判断あり・spec/14）。
- **循環回収**（Ref グラフ限定サイクルコレクタ・Bacon–Rajan trial deletion・設計は design-decisions 済）。
- **M3 パッケージ管理**（導入後に `@Std/Syntax` を in-tree→外部共有パッケージへ昇格）。
- **spec 自身が将来送り**＝固定長配列 `[E; N]`・const generics・`Slice`・部分文字列・`USize`/`ISize`。

## 再利用資産・罠（git で拾いにくい知見）

### フロントエンド・arena・型機構

- **型付き AST キャッシュ**：`typeOf(c, id)`＝codegen の型読み口。ノード型を `Comp` の **4 並列 scalar 配列**にキャッシュ（self-host は struct 配列 IndexSet 非対応ゆえ）。**body emission のエントリ（`genFunc`/`genClosure`/`genAsyncFunc`）で `clearExprTyCache`**＝env（Self/型 args/インスタンス）は 1 body の emission 中だけ一定なので grounded 型をその間だけキャッシュ・関数境界で破棄。`buildExprTyTable` は slot を allocate するだけで充填はしない（typeOf が emission 中に遅延充填）。
- **共有パーサ→arena lower**：共有 @Std/Syntax パーサが値ツリー（`TopItemAst`/`DeclAst`/…）を返し、`Codegen/Lower.pw` が arena（`Expr`/`Stmt`/`Func`/`StructDef`…）へ lower。**名前の offset 規律**＝`moduleOf` に効く名前（free fn 宣言名・call 名・extern fn 名・import/export 名）は**原本 offset 維持**、それ以外（struct/enum/field/型名・impl 系全部）は `internBytes` で再インターン。
- **block 0 は dummy 予約**：`body` フィールド 0 は「無本体」sentinel。entry ファイルは ambient Prelude より先に lower されるので予約しないと最初の関数本体が block 0 に来て無本体と誤判定。
- **名前/型 interning（Phase C＋D）**：`intern(c, start, len) -> U64`＝span の**内容**を 1-based id に。**0 は返さない**＝`nameId`/`tyNameId` の既定 0 が「未設定」sentinel。唯一の content-keyed テーブルを宣言名と型名が共有。`TypeRef.nameId` の choke point＝`pushType`。**罠**：①構築点を 1 つ漏らすと id=0 で converted スキャンが取りこぼす silent miscompile（「Bind を返すヘルパ」が raw `<Bind/>` で nameId を落とすと比較が恒偽）。②Local は動的＝`addLocalCn` で intern。③スキャンは「クエリを 1 回 intern→ループは整数 ==」。
- **演算子 opcode**：`op` フィールドは `kindCode(Kind)` の整数。`Ops.pw` の `opAdd()..` が単一の symbolic 源。
- **負リテラル畳み込みは lower で実装済**：`lowerExpr` が `Unary(Neg, IntLit)` を符号付き `Expr.Int`（`isNeg`）へ畳む（precedence 解決後ゆえ `-128.foo()` は real Neg）。新リテラル種を足すなら `Expr.Int` の全 match/構築点に `isNeg` を通す。
- **⚠ ambient Core に「よくある名前」を入れると衝突**：①bare 呼びは assoc fn を拾わない（`findFunc` は `hasRecv`＋`isAssoc` も skip・assoc は `Type.` 必須）。②conformance は型パラメータ arity も一致で照合。
- **missing-return 検査**（`Check.pw checkReturns`）：非 `()` 関数の本体が全経路で発散しなければ loud エラー（末尾式の暗黙 return なし）。
- **⚠ オーバーロード解決は局所 struct 引数の型を front-end で見れない**：`findFunc`/`paramsTypesMatch` は `exprType`（front-end）依存だが、局所変数は `c.locals` 非投入ゆえ struct 値の型が scalar 化＝ラベルのみで照合→first-by-label（順序依存）。型/ビューで選び分けるには backend で精緻化（`findFuncViewAware`）。新たに「引数の型/属性でオーバーロードを分ける」機能は同じ壁。
- **無名レコード = 合成 struct**：`lowerType` が record TypeAst を canonical 名 `(n:key,...)`（ソート済フィールド・`recordTypeKey`＝head＋arg-head のみ）の合成 StructDef に変換し既存 struct 機構を再利用。**construction-field/call-arg context**＝`makeFieldType`（owner 型名＋variant＋isEnum で field 宣言型を引く・owner decl は body より source 順で先に登録済）／`callArgType`（非 generic free fn を名前+label で一意解決）が dict/record/`()` literal に context を与える。罠＝generic owner では field 型が param ゆえ無型 int は grounding されない。
- **newtype = 別型・underlying 表現**：`Comp.newtypes` テーブル。`llvmScalarTy`/`binWitnessType`/`cmpOperandType` が newtype→underlying 解決（演算子は**両オペランド同一 newtype のときだけ**＝sound・result 型は operand newtype のまま＝Self 置換が自動）。`knownNamedTypeId` が distinct id（型区別）。非 int underlying のメソッド継承は `typeOf` 両用問題で codegen 専用解決点の個別配線が要る（未）。

### 値意味論・ARC・再帰値型

- **再帰値型 box**：`markBoxedFields`（`Mono.pw`・`Comp.boxedFields`＝field の `nameStart` キー）が循環を閉じるユーザー側フィールドを box（`Array`/`RawBuffer`/`Ref`/`WeakRef` は cut・`Optional`/`Result` は透過）。box＝`plew_arc_alloc` rc セル。
- **⚠ `Array[Enum]` の要素 deep copy/release**：`arrayElemNeedsDeep`＝`structNeedsCopy` **OR** `enumNeedsCopy`。enum 要素を取りこぼすと heap 持ち variant の `Array[Enum]`（`Array[ExprAst]` 等）が leak＋UAF。**新しく heap を持つ要素種を Array に入れるときは必ずここ**。
- **⚠ by-value 引数のローカルはコピーされず格納されうる**：ヒープ持ち値型の名前付きローカルを by-value で `append` 等に渡すと CoW コピーされず浅く格納→scope 末 release で dangling。**`xs.append(parseFoo())` と一時値で渡す**（根治＝by-value 引数 place の CoW コピー未対応）。
- **値意味論 ARC の在処**：concrete＝`emitFieldAction`、mono＝`emitMonoFieldAction`/`emitMonoModeDef`。**heap フィールド種を足すのはこの 2 dispatcher**。
- **give-block 脱糖**：値位置ブロック＝arena `IfExpr(cond=Int(1,isBool), thenBlk, elseBlk)`（C statement-expression）を合成し then で `mut val __t=…; …; give move __t`、else は dead だが型のため同型値を give。**所有権**＝`give move __t`（deep copy なし移譲・素の `give __t` は shallow＋ブロック末 release で UAF）。

### generic・Iterator 機構

- **generic struct field の array 単相化**：`Array[Box[T]]`/`Array[Entry[K,V]]` は `groundedArrayFieldInfo`（array 枝が要素を instance env で ground）＝`ref` 不変で波及ゼロ。template の非 ground compound 要素は runtime/method を emit しない。**罠**：型パラメータ名 vs 具体型名のグローバル衝突（ユーザー struct `B` と `MapIter[…,B]` の `B`）→ `isTypeParamName` は宣言済 struct/enum 名を具体型扱い。
- **Iterator/generic 機構**：method-level 型パラメータ（`FnInst.recvInstRef`・mangle は method-own tail のみ）・demand-driven 単相化（`providedRetReachable`）・Self/Item 解決（`curSelfRef`/`curItemRef`）・構造的型推論（`unifyTypeParam`）・推移的インスタンス探索（`scanGenInstMethodBodies`）。
- **Iterator 終端/adapter 機構**：終端 `count`/`fold[B]`/`first`/`reduce`/`collect`/`any`/`all`＝`pub impl Iterator` の provided method・lazy adapter（`MapIter`/`FilterIter`/`TakeIter`/`SkipIter`）受信で動く。**鍵**＝(1) **own-args sidecar** `st.genMethOwn`（gen-method slot を (mfi,instRef,ownArgs) でキー化・`fold[B]` の method-own B を `methodOwnArgs`/`inferFnArgs` で推論）。(2) **`resolveTy` last-match**（own param が conformer の同名 param を正しく shadow）。(3) **provided method の署名計算で Self/Item env**（`setSelfItemEnv`・戻り `Optional[Item]` を ground）＋`llvmFnRetTy` の grounding を `curItemRef`/`curSelfRef` でも発火。(4) 新 adapter は std struct＋`as Iterator`＋`adapterCallResultInst` の名前マッピング 1 行（generic 自動でない）。罠＝非スカラ accumulator の `fold` は collided env ゆえ loud reject（数値は OK）。
- **generic 呼び出しの引数 T 推論には arg 型に名前 span が要る**：`inferFnArgs`→`tyRefOfInfo` は `nameLen==0` で 0。リテラル等の typeOf が名前なし TypeInfo を返すと T 未推論。`Expr.Str`/String(kind 1) は名前 span 付与済。

### LLVM backend（`Backend/Llvm.pw`・ヘッダ＋コメントに詳細）

- **ディスパッチ**：ユーザー関数/メソッドは合成名 `pf<idx>`／generic は `gm<mfi>_<instRef>`＋index→キャッシュ `LLVMValueRef`。`c.locals` 非投入なので env に locKind/locStructIdx/locTypeRef を持たせて型判定。
- **narrowing `as` 不在**ゆえ LLVM API の count/index/aggregate-index 系 U32 パラメータは**バインディングで U64 宣言**。
- **match payload bind はフィールド名で punning**＝`val x` は「フィールド x を束縛」・別名は `field: val local`（bare `val fbase` は非存在フィールド punning で self-compile を壊す）。
- **aggregate（struct/enum/array）に icmp/extractvalue を非 aggregate へ誤適用すると LLVM builder が SEGV**＝必ず `LLVMGetTypeKind==10/exprIsArray` でガード。null sentinel は `LLVMIsNull`。
- **array は `{ptr,i64}`** で element-size generic runtime。配列リテラルは elemTy を束縛/param の annotation から取る（i64 デフォルト混入を防ぐ）。inout param/self は alloca せず**ポインタ param を locPtr 直結**。
- **メソッド emission をエントリモジュール限定に**＝std メソッド本体（未対応構文だらけ）を引き込まないよう、call-graph 到達可能性で必要な std だけ遅延 emit（`ensureProvidedDeclared`/`ensureGenMethodDeclared`/`ensureFreeFnDeclared`）。primitive lang-item は extern struct ゆえ nested-struct 判定で extern を除外。
- **⚠ alloca は全て entry ブロックへ**（`entryAlloca`＝entry の terminator 直前へ挿入）。builder 位置で alloca を吐くと loop body で反復ごとに stack が積もり大入力 SEGV（clang は entry へ hoist するので C から来ると起きない罠）。
- **⚠ inline enum payload のサイズ**：`fieldWords` は enum 型フィールドを `1 + enumPayloadWords(eid)` words と数える。過小だと大 variant 構築で隣接破壊。
- **checked 算術床**：add/sub/mul/div/rem/neg は runtime helper `plew_<w><Op>`（i8-i64 符号別・`__builtin_*_overflow`＋ガード）経由＝overflow/0除算 loud panic。`buildBinOpS` は wrapping 床（`wrapping*` 専用）。
- **`extern(c)` 不透明ハンドル型**（`StructDef.isCExtern`）は `llvmScalarTy` でも `ptr` に落とす（i64 既定だと invalid IR）。
- **for-over-array の element 追跡**：`genLlvmForArray` がループ変数を element の struct index＋TypeRef で束縛（field access/method dispatch が効く）。**primitive 要素は `structRegSlot` ゲートで除外**（scalar を struct 誤認しない）。generic-instance 要素は `genStructSlot`。
- **gen モード（`plew gen`）**：fn emission で `c.genMode` 時は `genMainIdx`（合成 harness main）だけを main 扱い。
- **検証配置の罠**：種ビルド binary は **std の 1 つ上**に置く（`computeStdRoot`＝binary dir＋`std/`）。

### 受理検査の共有パス（`Codegen/Verify.pw`／型チェッカ `Codegen/Infer.pw`）

- **検査は emitter のローカル表 `c.locals` に依存**ゆえ、独立 verify パスでも genFunc 同等に再構築必須（self 環境/curRet/param locals＋ブロックスコープ＋Let/For/Match-arm で `addLocal`）。
- **演算子witness 検査は `hasCompareWitness`/`hasBinTraitWitness` ガードを先に**（`binTraitNeedsTrait` はスカラ算術に常に true＝「witness 必須」の意・単独で呼ぶと valid 算術を誤 reject）。
- **match-arm bind は scrut の型で generic grounding**（`genericFieldTypeInfo`/`genericEnumFieldTypeInfo`）。
- **move 解析は分岐入口で `c.curBranchBase=scopeMark()`**（conditional-move 検出）。
- **verify は型回復後（`buildExprTyTable` の後＝runFrontend 末尾）で呼ぶ**。**async fn は除外**（`return n` が Promise 中身型を返すため）。
- **型チェッカ（Infer.pw）の `knownNamedTypeId` が型同一性の根**＝struct/enum/newtype に distinct id、`extern(c)`/float/`any P`/untyped int は判定せず（保守的）。newtype の型区別はここ。

## ビルド・テスト・機能追加手順

- **ビルド（self-host）**：`./bootstrap.sh`＝IR 種→clang＋libLLVM→`plewc0`→`_.pw` 自己コンパイル→IR 不動点 cmp。`./bootstrap.sh --reseed` で種更新（→ 種 `.ll`/`.runtime.c`＋追跡バイナリ `compiler/plewc` を commit）。編集中の高速反復は `./dev-rebuild.sh`（現 `compiler/plewc` が src を再コンパイルして自分をその場上書き）。**⚠ dev-rebuild は前回ビルドの plewc を使う**ので、miscompile を疑うときや verify 誤 reject で自己再ビルド不能なときは `./bootstrap.sh`（種から clean）で切り分け。**libLLVM がビルド必須依存**（IR 種は版に結合＝大改版で要 `--reseed`）。
- **テスト**：`./test.sh`（run/panic/reject/part＋不動点・メモリは `ASAN=1`〔macOS の ASan は leak 非対応〕）／`./test-gen.sh`（メタプロ gen/genreject）。reject は SKIP マスクなし（単一 backend）。
- **メタプロ生成**：`./plew-gen.sh <file.pw>`＝`plewc --gen <file>`→clang→run > `<file>.gen.pw`（`@[...]` 付きファイルに derive を走らせ生成 part を吐く・コミット・通常ビルドが auto-part）。
- **機能追加＝ADD→reseed→USE**：新機能を plewc.pw の**ソースで使う**には ①`compiler/src/`（または `compiler/std/`）に足す→②`--reseed` で種更新→③ソースで使う。**codegen 出力変化・AST フィールド追加・新 preamble 行・intercept 追加は reseed 2 回**、**codegen 出力を変えない検査追加は reseed 1 回**。
- **AST フィールド追加のコツ**：`val isPub: Bool = false` とデフォルトを付けると既存の構築点が省略で通る。**arena Expr/AST バリアント追加**は exhaustive な `match` 全箇所（`ExprAst`/`StmtAst` は `Lower.pw` の `lowerExpr`/`lowerStmt`・`TopItemAst` は `lowerTopItem`/`lowerExportItem`＋`Syntax.pw` の derive matcher）にケース追加＝reseed 2 回。
- **⚠ コンパイラは `@Std/Io`→`@Std/Core`／`@Std/Syntax`／`Prelude` を import** するので、これらも「コンパイラ自身がコンパイルするソース」。種がまだ受理しない構文を std で使うと bootstrap が即壊れる＝機能を先に種へ焼く。
- **⚠ コンパイラ内で extern intrinsic を使う罠**：`extern "plew-intrinsic"` を **compiler 自身が呼ぶ**（std でなく）と、genLlvmCall の name-intercept を経ず undefined シンボルになり得た（`truncU8` で発生）。compiler が使う truncation 等は runtime `plew_*` シンボル化を検討。

## 運用メモ

- 動作する区切りで**記述的 git タグ**。コミットメッセージは英語＋`Co-Authored-By`。GPG 署名（`--no-gpg-sign` を使わない・pinentry タイムアウトは再実行で通る／詰まったら手元で `echo test | gpg --clearsign` でパスフレーズキャッシュ／**コミットメッセージにバッククォートや `<…/>` を書くと zsh がコマンド置換/リダイレクトで壊すので避ける・`-F` でファイル渡しか平文化**）。
- scratch は repo 直下の `tmp/`（gitignore 済）。直接コマンドに `rm` を書くと毎回確認が出るので cleanup はスクリプトに内包。
- 不要になった実装は削除し、必要なら git tag から復旧（例：`git checkout stage0-final -- bootstrap`）。
- 言語表面の決定は spec（正典）＋claude/ に即同期し、grep で陳腐化を点検。
