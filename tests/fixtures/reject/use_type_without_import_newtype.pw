// Scoped resolution (spec/15), newtype-underlying site: the underlying type of a
// `newtype` must be in scope. `Tok` (@Plew/Syntax) is loaded via `kindCode` but not
// imported as a type, so naming it as the underlying must be rejected.
import @Plew/Syntax with { kindCode }

newtype WrappedTok = Tok

fn main() {
}
