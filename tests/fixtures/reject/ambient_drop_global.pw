import @Std/Io with { print }
mut val value = 7I64
unique struct Observer {
    pub val unused: I64
    deinit { print(value) }
}
pub impl Observer { factory }
fn update(destination: inout I64) {
    val observer = <Observer unused=0I64 />
    destination += 1I64
}
fn main() {
    update(destination: inout value)
    print(value)
}
