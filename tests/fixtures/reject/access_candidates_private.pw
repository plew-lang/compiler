import /Ir with { Decls, AccessCandidate }
fn main() {
    mut val declarations: Decls = <Decls />
    declarations.accessCandidateRows.append(<AccessCandidate readExpr=0U64 placeExpr=1U64 writeExpr=2U64 />)
}
