import @Std/Io with { print }
// `a[k]` needs an Index implementation (spec/12); a plain struct has none.
struct Foo { pub val n: I64 }
pub impl Foo { factory }

fn main() {
    val f: Foo = <Foo n=5 />
    print(f[0I64])
}
