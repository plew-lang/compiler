// A record-destructure shorthand bind must spell a field's name: `name2` is not
// a field of the record, so this must be rejected (not silently mis-bound).
fn makeRec(a: I64) -> (name: String, n: I64) {
    return (name: "s", n: a)
}

fn main() {
    (val name2, val n2) = makeRec(a: 2)
}
