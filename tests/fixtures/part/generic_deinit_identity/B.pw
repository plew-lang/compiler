import @Std/Io with { print }
// Same spelling in another module must never select this destructor.
unique struct Resource[T] { pub val value: T deinit { print("second") } }
pub impl[T] Resource[T] { factory }
pub fn second() { val item = <Resource[I64] value=1I64 /> }
