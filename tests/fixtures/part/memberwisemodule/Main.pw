import @Std/Io with { print }
import ./Lib with { Secret }
fn main() {
    val first = <Secret value=3I64 />
    val second = <Secret value=4I64 observed=6I64 />
    print(first.sum())
    print(second.sum())
}
