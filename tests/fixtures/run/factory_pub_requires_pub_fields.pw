// Module-scoped visibility permits this use; construction and later access are independent.
import @Std/Io with { print }
struct Box {
    pub val n: I64
    pub(get) mut val hidden: I64
}
pub impl Box { factory }
fn main() { val box = <Box n=1 hidden=2 /> print(box.hidden) }
