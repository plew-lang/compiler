import @Std/Io with { print }
import @Std/Async with { sleep }

unique struct Resource {
    pub val number: I64
    deinit { print(99I64) }
}

pub impl Resource { factory }

async fn main() {
    val resource = <Resource number=7I64 />
    await sleep(ms: 1)
    print(resource.number)
}
