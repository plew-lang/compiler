import @Std/Io with { print }
unique struct Resource[T] {
    pub val value: T
    deinit { print("drop") }
}
pub impl[T] Resource[T] { factory }
fn main() {
    val item: Resource[I64] = <Resource value=7I64 />
    print("body")
}
