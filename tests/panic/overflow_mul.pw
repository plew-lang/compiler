// Multiplication overflow panics too. U16 max is 65535; 1000 * 1000 = 1_000_000
// does not fit, so the checked multiply aborts.
import @Std/Io with { print }

fn main() {
    val a: U16 = 1000
    val b: U16 = 1000
    print((a * b) as U64)
}
