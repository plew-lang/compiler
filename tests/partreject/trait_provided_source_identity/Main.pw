import ./Zero with { Clash as ZeroClash }
import ./One with { Clash as OneClash }

struct S {}
pub impl S { factory }
pub impl S as ZeroClash {}
pub impl S as OneClash[I64] {}

// Source spelling alone cannot merge the two provided surfaces.
fn main() {
    val s: S = <S />
    s.pick()
}
