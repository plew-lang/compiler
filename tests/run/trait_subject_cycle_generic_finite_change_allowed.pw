import @Std/Io with { print }

// This owner revisit is finite: only A[I64] matches the first edge, and the
// path ends at A[String].  It neither closes exactly nor self-embeds a binder.
trait A[Item] {}
trait B {}

impl A[I64] as B {}
impl B as A[String] {}

struct S {}
pub impl S { factory }
pub impl S as A[I64] {}

fn main() {
    print(1I64)
}
