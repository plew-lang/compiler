import @Std/Io with { print }

// A cycle confined to an extension is inert until the extension is selected.
// Its declaration alone must remain valid.
trait A {}
trait B {}

extension E {
    impl A as B {}
    impl B as A {}
}

fn main() {
    print(1I64)
}
