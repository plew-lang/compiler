// A non-runtime plewIntrinsic extern must remain a Direct CallTarget while
// runtime ABI classification is carried by the finalized declaration.
import @Std/Io with { write }
import @Std/Process with { argCount }

fn main() {
    val count: I64 = argCount()
    if count > 0I64 { write(s: "process\n") }
}
