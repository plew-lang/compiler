// A struct with two fields of the same name makes construction/access ambiguous — reject.
struct A {
    pub val x: I64
    pub val x: I64
}
pub impl A { factory }

fn main() { val a: A = <A x=1 /> }
