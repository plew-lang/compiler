// spec/04: a named function declaration is callable, but not a value. A first-class
// function must be an explicit closure wrapper, not a bare declaration name.
fn twice(x: I64) -> I64 { return x * 2I64 }
fn main() {
    val f: fn(x: String) -> String = twice
}
