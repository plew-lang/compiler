// `assert` is a @Std/Core function, not a lang item: even though Core is ambient
// (its operators / Optional / Result are usable without import), `assert` must be
// imported (spec/15). Using it without `import @Std/Core with { assert }` is rejected.
fn main() {
    assert(true)
}
