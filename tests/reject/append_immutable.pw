// `.append` mutates the array, so the receiver must be a mutable binding.
// Appending to a plain `val` array must be rejected.
import @Std/Io with { print }

fn main() {
    val xs: Array[I64] = [1, 2]
    xs.append(3)
    print(xs.count)
}
