struct S {
    mut val x: I32
}

struct T {
    mut val s: S
}

fn main() {
    mut val a = <T s=<S x=42 /> />
    sync val b = a

    spawn {
        b.lock().s.x += 1
    }

    a.s.x += 1

    print("{a.s.x}")
}
