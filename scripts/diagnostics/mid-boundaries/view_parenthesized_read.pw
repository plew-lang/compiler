import @Std/Io with { print }
struct Counter { pub val value: I64 }
pub impl Counter { factory }
extension Readable {
    pub impl Counter { fn inspect() -> I64 { return self.value } }
}
fn main() {
    val counter = <Counter value=10I64 />
    print((counter#Readable).inspect())
}
